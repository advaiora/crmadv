# Handoff — staffetta del 14 luglio 2026, ore 15:51

**Scritto da:** Jacopo
**Per chi riprende:** con ogni probabilità ancora Jacopo. Se ci mette mano **Claudio** nel weekend: leggi l'avviso database qui sotto, c'è **una migrazione nuova**.

---

## ⚠️ DATABASE — LEGGERE PRIMA DI TUTTO

In questa sessione ho aggiunto **una nuova migrazione**: `20260714131252_ai_collaborative_chat`. Trasforma la chat di progetto da "una conversazione privata per ogni utente" a "una conversazione **condivisa** per progetto": crea tre nuove tabelle (`AiConversation`, `AiConversationParticipant`, `AiConversationMessage`) e **rimuove** la vecchia tabella `ProjectChatMessage`. La chat vecchia (per-utente) è stata **azzerata di proposito**: era prevista dalla specifica, non c'erano dati reali.

Come sempre la migrazione va applicata **dopo** l'arretrato `20260706085001`.

**Come allinearsi (invariato):**
- In sviluppo, potendo perdere i dati locali: `npx prisma migrate reset` poi `npm run db:seed:demo`.
- Conservando i dati: `npx prisma migrate deploy` (se l'arretrato non è ancora "risolto", prima `npx prisma migrate resolve --applied 20260706085001`).

Sul mio ambiente è già applicata e il client Prisma è rigenerato. Promemoria avvio: login admin `admin@test.com / admin123`; utenti demo membri con password `demo123`; l'AI risulta ancora **"non configurata"** finché non si mette una chiave in Impostazioni Agency.

## Cosa ho fatto

Due blocchi, entrambi verificati. **Non ancora committato** al momento della scrittura (committo e pusho subito dopo questo handoff).

**1) Audit grafico dell'area Agency (chiaro/scuro) — chiuso.**
Ho eliminato i "box e campi bianchi in tema scuro" che restavano nell'area Agency. La causa era il foglio di stile dell'area (`src/views/Agency/agency-ui.css`) che scriveva i colori "a mano" (bianco, grigi fissi) senza versione per il tema scuro: l'ho riscritto usando i colori-tema del progetto (i "token"), così chiaro e scuro si adattano da soli. Sistemato anche un riquadro "nessun URL" nella scheda Fonti che restava chiaro. Verificato su **tutte** le schede di progetto (14) + lista progetti + Impostazioni Agency: in tema scuro **nessuno sfondo resta bianco**, e il tema chiaro è rimasto identico a prima.

**2) Chat AI collaborativa — Fase 1 fatta e verificata.**
La chat dentro ogni progetto Agency ora è **condivisa su invito** invece che privata. Novità concrete:
- **Un solo thread per progetto**, condiviso tra i partecipanti invitati. Chi apre per primo la chat ne diventa il "proprietario".
- **Autore mostrato su ogni messaggio** (i propri a destra, quelli degli altri e dell'AI a sinistra con il nome).
- **Invito dei partecipanti**: pannello "Partecipanti" con l'elenco (con ruolo) e un menù per invitare altri membri del workspace; si può anche uscire/rimuovere.
- **L'AI risponde solo se interpellata**: scrivendo `@AI` nel messaggio oppure col pulsante **"Chiedi all'AI"**. Il pulsante normale **"Invia"** posta solo il messaggio al team, senza far intervenire l'AI.
- Chi non è stato invitato non vede i messaggi e non può scrivere.

File principali toccati: schema del database `prisma/schema.prisma` + nuova migrazione; nuovo `server/repositories/ai-conversation.repository.ts` (gestione dati conversazioni, sostituisce il vecchio `project-chat.repository.ts` che ho rimosso); logica in `server/modules/agency-os/agency.service.ts` e nuove rotte in `server/modules/agency-os/routes/workspace-agency.route.ts`; lato interfaccia `src/views/Agency/project/AgencyProjectChatPage.jsx` più i due file che parlano con l'API (`src/modules/agency-os/api/agency.api.js` e `.../data/agencyDataAdapter.js`).

## Dove mi sono fermato

- **Audit grafico:** completo, niente in sospeso.
- **Chat Fase 1:** completa e provata a fondo dal lato server (creazione conversazione, invito, un secondo utente invitato che vede e scrive, un utente non invitato correttamente bloccato, autore corretto sui messaggi) e verificata anche a schermo (la pagina si apre senza errori, mostra autori, partecipanti e i due pulsanti). Controllo TypeScript senza nuovi errori (restano i 233 preesistenti); interfaccia con lint pulito sui file toccati.
- **Le risposte dell'AI non sono ancora collaudabili davvero:** senza una chiave AI reale l'assistente non genera. Tutta la parte "collaborativa" (scrivere in gruppo, inviti, autore) funziona lo stesso; la generazione va riprovata quando si mettono le chiavi.
- **Dati di prova nel database demo:** ho lasciato una conversazione di test con qualche messaggio e un membro (Giulia) invitato. Si possono azzerare dal pulsante "Azzera" dentro la chat, oppure ripartono puliti con un reset+seed.

## Come riprendere — prossimi passi

1. **`git pull`** per avere gli ultimi commit.
2. **Allinea il database** (vedi avviso in cima): applica la migrazione `20260714131252_ai_collaborative_chat`.
3. Avvio (due terminali): `npm run dev:api` (porta 4000) e `npm run dev` (porta 5173). La chat è dentro un progetto: Agency → Progetti → apri un progetto → scheda **Chat**.
4. **Quando si continua con la Chat collaborativa:** seguire il piano a fasi in `archivio-documenti/spec-chat-ai-collaborativa.md`, prossima è la **Fase 2** (ambiti Generale/Cliente/Progetto + popup globale + selettore "assegnati a me"). Attenzione: la Fase 2 si appoggia al **modello di assegnazione/visibilità utente↔progetto introdotto in V2**. **Nota di metodo:** prima di trattare V2 (o qualunque area) come "lavoro di qualcun altro", **verificare con l'utente chi l'ha fatta** — non darlo per scontato.
5. **A fine V:** configurare le **chiavi reali OpenAI/Anthropic** e collaudare le risposte dell'AI nella chat (e nelle altre funzioni AI).

## Riferimenti utili

- **Specifica della feature (fonte di verità):** `archivio-documenti/spec-chat-ai-collaborativa.md` (spec + piano a 6 fasi).
- **Chat condivisa:** interfaccia `src/views/Agency/project/AgencyProjectChatPage.jsx`; dati `server/repositories/ai-conversation.repository.ts`; logica `server/modules/agency-os/agency.service.ts`.
- **Audit grafico:** `src/views/Agency/agency-ui.css`; regole colore/tema in `archivio-documenti/design-system-temi.md` e `design-linguaggio-apple-web.md`.
- **Note operative** (`archivio-documenti/note-operative-ai.md`): aggiunte due voci utili — su Windows fermare l'API prima di rigenerare Prisma (lock file), e per i test via `curl` le rotte Agency vogliono l'header `x-workspace-id`.
