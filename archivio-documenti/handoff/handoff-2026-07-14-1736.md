# Handoff — staffetta del 14 luglio 2026, ore 17:36

**Scritto da:** Jacopo
**Per chi riprende:** con ogni probabilità ancora Jacopo. Se ci mette mano **Claudio** nel weekend: leggi l'avviso database qui sotto — ci sono **due migrazioni** da applicare.

---

## ⚠️ DATABASE — LEGGERE PRIMA DI TUTTO

In questa sessione ho aggiunto **una nuova migrazione**: `20260714170302_ai_conversation_client_general_scope`. Aggiunge un campo (`clientId`) e alcuni indici alla tabella delle conversazioni della chat: serve per far esistere la chat "a livello Cliente" e quella "Generale". È una modifica **additiva**: non tocca né cancella dati esistenti.

Come sempre va applicata **dopo** l'arretrato `20260706085001`. Attenzione: se riprende **Claudio**, sul suo ambiente potrebbero mancare **due** migrazioni della chat: prima `20260714131252` (la Fase 1, dalla sessione precedente) e poi questa `20260714170302`.

**Come allinearsi:**
- In sviluppo, potendo perdere i dati locali: `npx prisma migrate reset` poi `npm run db:seed:demo`.
- Conservando i dati: `npx prisma migrate deploy` (se l'arretrato non è ancora "risolto", prima `npx prisma migrate resolve --applied 20260706085001`).
- Nota pratica (Windows): prima di rigenerare il client Prisma va **fermato il server API**, altrimenti dà errore per un file bloccato. La procedura non interattiva per creare/applicare migrazioni dalla sessione AI è annotata in `note-operative-ai.md` (voce 16).

Sul mio ambiente la migrazione è **già applicata** e il client Prisma è già rigenerato. L'AI risulta ancora **"non configurata"** finché non si mette una chiave in Impostazioni Agency. Login admin: `admin@test.com / admin123`; membri demo con password `demo123`.

## Cosa ho fatto

**Fase 2 della Chat AI collaborativa — fatta e verificata, già committata e pushata su `main`** (commit `baa1111`).

La Fase 2 porta la chat **fuori dalla singola scheda di progetto**: ora è raggiungibile ovunque e ha tre "ambiti".

**1) Popup di Chat AI globale.** Un pannello che si apre da un'icona in alto (nella barra, accanto alla ricerca) su **qualsiasi pagina** del CRM. Stessa logica della palette comandi (Cmd/K). File nuovo: `src/views/Agency/chat/AiChatWidget.jsx`; agganciato nello "scheletro" comune delle pagine (`src/layout/MainLayout/ClassicLayout`) e con l'icona in `src/layout/Header/TopNav.jsx`. Le bolle dei messaggi sono state spostate in un file condiviso (`src/views/Agency/chat/chatBubble.jsx` + `chatShared.js`) così popup e scheda progetto usano lo stesso identico componente.

**2) Selettore d'ambito: Progetto / Cliente / Generale.**
- **Progetto**: come prima, ma dal popup; apre **la stessa conversazione** della scheda Chat del progetto (nessuna duplicazione). In cima al selettore compaiono i **progetti assegnati a me**, sfruttando la visibilità della V2 (chi è owner o assegnato al progetto, o capo del reparto; chi può "vedere tutto" li vede tutti).
- **Cliente**: chat su **tutti i progetti di quel cliente insieme** — l'AI cerca nelle Fonti di tutti i suoi progetti. I clienti mostrati sono ricavati dai progetti che l'utente può vedere (nella V2 non esiste un'assegnazione diretta utente-cliente, quindi si derivano dai progetti).
- **Generale**: chat libera con l'AI, senza un progetto o cliente specifico.

**File principali toccati (backend):** `server/repositories/ai-conversation.repository.ts` (conversazioni per cliente/generale), `server/modules/sources/sources.rag.ts` (ricerca sulle fonti di più progetti), `server/modules/agency-os/agency.service.ts` e `agency.repository.ts` (logica e query), nuove rotte in `server/modules/agency-os/routes/workspace-agency.route.ts`. **Frontend:** il widget sopra + `src/modules/agency-os/api/agency.api.js` (chiamate ai nuovi indirizzi).

## Dove mi sono fermato

- **Fase 2: completa.** Verificata a fondo: endpoint provati via `curl` (visibilità "assegnati a me", chat cliente e generale che salvano i messaggi), e i **tre ambiti provati a schermo** nel browser (popup che si apre ovunque, selettore, conversazioni che caricano). Controllo TypeScript senza nuovi errori (restano i 233 preesistenti); lint frontend pulito sui file toccati; tema chiaro/scuro a posto.
- **Le risposte dell'AI non sono ancora collaudabili davvero:** senza una chiave AI reale l'assistente non genera. Tutta la parte "collaborativa" e la scelta degli ambiti funzionano lo stesso; la **ricerca sulle fonti (RAG) dell'ambito Cliente** è scritta e senza errori ma va provata quando ci saranno le chiavi.
- **Database demo pulito:** le conversazioni di prova che ho creato durante le verifiche le ho cancellate. È rimasta intatta la conversazione di test della sessione precedente (quella con Giulia invitata).

## Come riprendere — prossimi passi

1. **`git pull`** per avere l'ultimo commit (`baa1111`).
2. **Database:** se sei Jacopo è già a posto. Se sei Claudio, applica le migrazioni (vedi avviso in cima).
3. **Avvio** (due terminali): `npm run dev:api` (porta 4000) e `npm run dev` (porta 5173). La chat globale si apre dall'icona in alto a destra, su qualsiasi pagina; la scheda Chat resta dentro ogni progetto (Agency → Progetti → apri un progetto → **Chat**).
4. **Prossima fase della Chat (Fase 3):** allegati (file ed elementi del CRM) e "Chiedi all'AI" dal menu azioni degli elementi. Piano completo in `archivio-documenti/spec-chat-ai-collaborativa.md` (Fasi 1 e 2 ora segnate come fatte; restano 3, 4 websocket, 5 compressione contesto, 6 navigazione suggerita).
5. **A fine V:** configurare le **chiavi reali OpenAI/Anthropic** in Impostazioni Agency e collaudare le risposte dell'AI in tutti gli ambiti (progetto, cliente, generale).

## Riferimenti utili

- **Specifica della feature (fonte di verità):** `archivio-documenti/spec-chat-ai-collaborativa.md`.
- **Popup chat globale:** `src/views/Agency/chat/AiChatWidget.jsx` (+ `chatBubble.jsx`, `chatShared.js`, `ai-chat-widget.css`).
- **Backend ambiti:** metodi `*ScopedChat` in `server/modules/agency-os/agency.service.ts`; rotte `/agency/chat/client/:clientId` e `/agency/chat/general` in `workspace-agency.route.ts`.
- **Note operative** (`archivio-documenti/note-operative-ai.md`): aggiunte due voci — come creare una migrazione dalla sessione AI senza comandi interattivi (voce 16) e come non auto-terminare lo script quando si fermano processi per nome (voce 17).
