# Handoff — staffetta del 13 luglio 2026, ore 12:16

**Scritto da:** Jacopo
**Per chi riprende:** con ogni probabilità ancora Jacopo. Se invece ci mette mano **Claudio** nel weekend: le note sul database qui sotto sono per te — ci sono **due migrazioni nuove** da applicare.

---

## ⚠️ DATABASE — LEGGERE PRIMA DI TUTTO

In questa sessione ho aggiunto **due nuove migrazioni** (modifiche allo schema del database), da applicare **dopo** l'arretrato `20260706085001` come sempre:

1. `20260713074114_project_source_chunks` — tabella per gli "spezzoni" vettorizzati delle Fonti (serve al RAG). Contiene una colonna speciale di tipo `vector` (estensione pgvector): la migrazione include già `CREATE EXTENSION IF NOT EXISTS vector`, quindi è autosufficiente, **ma richiede che pgvector sia installato** sul Postgres (vedi sotto).
2. `20260713094017_ai_budget` — tabella per i limiti di spesa AI (budget giornaliero per dipendente).

**Come allinearsi (invariato):**
- In sviluppo, se puoi perdere i dati locali: `npx prisma migrate reset` poi `npm run db:seed:demo`.
- Conservando i dati: prima `npx prisma migrate resolve --applied 20260706085001` (se non già fatto), poi `npx prisma migrate deploy`.

**Per Claudio in particolare:** sul tuo PC pgvector va installato una volta (serve permesso da amministratore). C'è lo script pronto `scripts/install-pgvector-win.ps1` — la procedura è nell'handoff del 10/7. Sul mio ambiente pgvector 0.8.0 è già installato e attivo.

Promemoria: password demo `admin123`; nel `.env` serve `PLATFORM_ADMIN_EMAILS="superadmin@demo.local"` (non è nel repo). Nel `.env` **non c'è una chiave OpenAI**: senza quella, indicizzazione e ricerca semantica girano "a vuoto" senza errori — vanno configurate in Impostazioni Agency per lavorare davvero.

## Cosa ho fatto

Ho completato **tutta la parte RAG della V4** (la ricerca intelligente sulle Fonti) e aggiunto il **controllo dei costi AI**. Tutto verificato con i test automatici; tutto pushato su `main`.

**1) Ricerca semantica sulle Fonti (RAG) — completa.**
Ora il testo delle Fonti di un progetto viene spezzato in blocchi e trasformato in "vettori" (numeri che rappresentano il significato), salvati nel database con pgvector. Data una domanda, il sistema trova i blocchi più pertinenti. Files nuovi principali: `server/modules/sources/sources.rag.ts` (spezzettamento, generazione vettori con OpenAI, ricerca) e `server/modules/sources/sources.indexing.ts` (aggancio automatico). Nota: i vettori si generano **solo con OpenAI** (Anthropic non ha questa funzione); Claude resta per la scrittura dei testi.

**2) Indicizzazione automatica + uso nel motore AI.**
Quando aggiungi/aggiorni una Fonte, viene indicizzata da sola (se la chiave OpenAI è configurata). Il motore AI, quando genera il brief Discovery, ora riceve i **passaggi più pertinenti** delle Fonti invece del solo testo troncato. Nel pannello Fonti ho aggiunto: un badge "Indicizzata / N blocchi" per ogni fonte e un pulsante **"Reindicizza tutte"** (utile se configuri la chiave OpenAI dopo aver già caricato le fonti). Il costo dei vettori viene registrato nel log costi AI esistente.

**3) Controllo costi AI — budget giornaliero per dipendente.**
Nuova funzione: si può impostare un **tetto di spesa AI al giorno** per ogni dipendente (con un default di workspace). Se un dipendente supera il limite, le sue azioni AI vengono **bloccate** con un messaggio chiaro (niente spesa oltre il tetto). La configurazione è in **Impostazioni Agency** (nuovo pannello "Budget AI giornaliero", visibile ai super-admin del workspace). File nuovi: `server/repositories/ai-budget.repository.ts`, `src/views/Agency/AgencyAiBudgetPanel.jsx`; il controllo vero è nel motore AI (`server/modules/agency-os/agency.service.ts`).

**4) Console Piattaforma — decisione presa e piccolo spostamento.**
Avevo valutato se il nuovo budget rendesse ridondante la sezione "Consumi & costi AI" della Console Piattaforma. **No:** quella è monitoraggio storico su tutti i workspace (per super-admin di piattaforma), il budget è controllo per singolo workspace/dipendente — sono complementari. Quindi **non ho toccato la Console** (è lavoro di Claudio). Ho solo **spostato la voce "Console piattaforma" in cima** al menu laterale (`src/layout/Sidebar/SidebarMenu.jsx`), come richiesto.

## Dove mi sono fermato

- **Tutto committato e pushato, albero pulito.** Niente lasciato a metà.
- **RAG completo e testato**, ma **non ho potuto verificarlo visivamente nel browser** (richiede login e una chiave OpenAI reale, che non è nel `.env`). Le verifiche fatte sono: test automatici tutti verdi (185 unit + 4 di integrazione sul database reale) e conferma che frontend e backend compilano.
- **Budget AI**: enforcement e configurazione fatti. Manca (di proposito) mostrare la stima di costo su **ogni** pulsante AI: per ora il costo è tracciato e il budget blocca, ma il "quanto costa questa azione" non è ancora scritto accanto a ogni bottone.
- **Nota su TypeScript:** restano i 233 errori TypeScript **preesistenti** del motore AI (non miei); ho verificato che le mie aggiunte non ne introducono di nuovi.

## Come riprendere — prossimi passi

1. **`git pull`** per avere gli ultimi commit.
2. **Allinea il database** (vedi avviso in cima): applica le due nuove migrazioni. Se sei Claudio e non hai pgvector, installalo prima con lo script.
3. Avvio (due terminali): `npm run dev:api` (porta 4000) e `npm run dev` (porta 5173). Login: `admin@test.com / admin123`.
4. **Per vedere il RAG lavorare davvero** serve una **chiave OpenAI**: mettila in Impostazioni Agency, poi apri un progetto → Fonti → carica un testo/PDF e premi "Reindicizza tutte"; dovresti vedere il badge "Indicizzata / N blocchi".
5. **Prossimo pezzo di V4 suggerito:** mostrare la **stima di costo su ogni pulsante AI**, ed eventualmente estendere l'uso del RAG anche agli altri percorsi AI (non solo il brief Discovery). In alternativa, chiudere le rifiniture visive della V4.

## Riferimenti utili

- **Ricerca semantica Fonti (RAG):** `server/modules/sources/sources.rag.ts` (logica) + `sources.indexing.ts` (aggancio automatico); interfaccia `src/modules/sources/ui/ProjectAiSourcesPanel.jsx`.
- **Uso del RAG nel motore AI:** `server/modules/agency-os/agency.service.ts` (funzione che recupera gli estratti pertinenti per il brief).
- **Budget AI:** `server/repositories/ai-budget.repository.ts` + controllo nel motore AI; interfaccia `src/views/Agency/AgencyAiBudgetPanel.jsx`.
- **Test di integrazione nuovi:** `server/integration/sources-rag.smoke.ts` e `server/integration/ai-budget.smoke.ts` (girano contro il database, saltano da soli se pgvector manca). Comando: `npm run test:backend`.
