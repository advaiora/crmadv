# Handoff — staffetta del 3 luglio 2026, ore 10:45

**Scritto da:** Jacopo
**Per chi riprende:** la prossima sessione (Jacopo nei giorni feriali, Claudio nel weekend)

## Cosa ho fatto

Sessione sull'aspetto Apple-style, in continuità con l'handoff precedente. **Attenzione: tutto è salvato su disco ma NON è ancora committato né pushato** (vedi "Dove mi sono fermato").

1. **Realizzata la Fase 1 (il "ri-vestito" solo-CSS), come concordato.** Nuovo file `src/styles/scss/apple-foundation.css` (un unico foglio di stile che dà a tutta l'app l'aspetto Apple da un punto solo), importato in `src/main.jsx` subito dopo `globals.css`. Cosa fa: mette il bordo "vetro" come default su tutte le superfici a scatola (`.card`), risolve una volta sola il "doppio bordo" delle card del template (`.card-border`) e sistema piccoli dettagli (campi con addon, accordion, pannelli laterali). Non tocca nessuna struttura delle pagine.

2. **Portato il test "separatori al posto delle scatole" sulla Dashboard.** I blocchi perdono la scatola e sono raggruppati con spazio + linee sottili (stile iOS Impostazioni/Mail). Si attiva con una sola classe (`dashboard-flat`) ed è reversibile togliendola. Verificato in chiaro e scuro.

3. **Dopo il tuo via libera (confronto con Claudio già fatto), esteso a tutto il CRM l'effetto vetro che "reagisce" al mouse.**
   - Nuovo componente `src/components/effects/GlassPointer.jsx` (un piccolo pezzo di codice montato nella struttura base dell'app): fa sì che il riflesso sul bordo vetro si accenda **dove si trova il cursore** e lo segua. Rispetta l'impostazione di sistema "riduci animazioni" (in quel caso non insegue nulla).
   - **Bordi di TUTTE le card** (sia quelle nuove sia quelle vecchie del template Bootstrap) ora hanno questo riflesso reattivo. A riposo è invisibile: identico a prima, nessuna regressione.
   - **I separatori della Dashboard** non sono più semplici linee, ma "linee-vetro" con lo **stesso materiale** del bordo delle scatole e la **stessa reattività al mouse** (più intenso dove passa il cursore), come richiesto.

4. **Aggiornato `archivio-documenti/note-operative-ai.md`** con una nota utile: nel progetto convivono due tipi di "card" (quelle del template Bootstrap `.card` e le nostre primitive React che rendono `div.glass-edge`); vanno mirate col selettore giusto.

## Dove mi sono fermato

- **Nulla è committato/pushato: l'albero di lavoro è "sporco".** File modificati: `note-operative-ai.md`, `src/main.jsx`, `src/styles/scss/globals.css`, `src/layout/MainLayout/ClassicLayout/index.jsx`, `src/modules/dashboard/ui/WidgetRenderer.jsx`, `src/modules/dashboard/ui/KpiCard.jsx`. File nuovi: `src/styles/scss/apple-foundation.css`, `src/components/effects/GlassPointer.jsx`. **Prima cosa consigliata alla ripresa: decidere se committare come punto di ripristino.**

- **Problema aperto con l'anteprima locale.** Ho verificato che l'effetto funziona ispezionando gli stili applicati (il riflesso si accende davvero dove passa il mouse, sia sui bordi delle card sia sui separatori), **ma non ho potuto fare screenshot**: lo strumento di anteprima va in timeout e nemmeno tu riuscivi a vederlo bene. Da chiarire in una nuova sessione **se è solo un problema dello strumento o una vera lentezza di resa** (l'effetto usa molti elementi decorativi con gradienti/maschere applicati a tutta l'app). È il motivo per cui abbiamo deciso di fermarci e ripartire da una sessione pulita.

- **Rimasto in sospeso, da fare/decidere alla ripresa:**
  1. **Calibrazione dell'effetto ("quanto si deve sentire").** Domanda lasciata aperta apposta, da rivedere guardandolo dal vivo: **sobrio come ora** (a riposo quasi invisibile) / **più percepibile** / **marcato "vetro evidente"**. I valori attuali da cui partire sono nei "Riferimenti".
  2. **Rollout progressivo del layout "senza scatole" alle altre pagine.** Ordine concordato: prima **Impostazioni** e **Checklist** (già "pulite", usano le primitive nuove), poi le pagine più dense.
  3. **Regola concordata per le pagine dense (es. Clienti): approccio MISTO.** Dove ha senso si tolgono le scatole e si usano i separatori; per gli elementi molto densi (tabelle, form) si **mantiene la scatola** applicando solo l'effetto vetro reattivo al mouse. Da applicare quando si arriva a quelle pagine, per non rendere l'esperienza confusa.

## Come riprendere — prossimi passi

1. **Leggi questo handoff** e, prima di rimettere mano all'aspetto, la bussola di design `archivio-documenti/design-linguaggio-apple-web.md`.
2. **Decidi se committare/pushare** quanto già fatto (vedi lista file sopra), così hai un punto di ripristino prima di continuare.
3. **Avvia l'app** (due terminali): `npm run dev:api` (backend, porta 4000) e `npm run dev` (frontend, porta 5173). Login di prova: **admin@test.com / admin123**. **Verifica per prima cosa se l'anteprima si vede bene**: se resta lenta/non renderizza, indaga se dipende dallo strumento di anteprima o dall'effetto (molti pseudo-elementi con maschere).
4. **Calibra l'effetto vetro**: guardalo dal vivo muovendo il mouse su tessere KPI, separatori e bordi delle card, poi scegli il carattere (sobrio / più percepibile / marcato) e ritocca i valori indicati nei "Riferimenti".
5. **Estendi il layout senza scatole** a Impostazioni e Checklist; per Clienti e pagine dense usa l'**approccio misto** descritto sopra.
6. Quando tocchi stili, lancia `npm run lint:css` e `npm run lint:colors` (ora 0 errori). Nota: `npm run lint` generale è rotto da solo (manca la config nel formato nuovo di ESLint 9, problema preesistente, non introdotto da noi).
7. A fine sessione, genera il nuovo handoff con `/handoff`.

## Riferimenti utili

- **File nuovi/toccati in questa sessione:**
  - `src/styles/scss/apple-foundation.css` — il layer di fondazione Fase 1 + le regole del test "separatori" e dei separatori-vetro.
  - `src/components/effects/GlassPointer.jsx` — il codice che fa seguire il riflesso al mouse; montato in `src/layout/MainLayout/ClassicLayout/index.jsx`.
  - `src/styles/scss/globals.css` — qui vive la classe del bordo vetro `.glass-edge`, ora con il riflesso reattivo al mouse.
  - `src/modules/dashboard/ui/WidgetRenderer.jsx` e `KpiCard.jsx` — hanno le classi che attivano separatori e reattività sulla Dashboard.
- **Valori di calibrazione attuali (le "manopole" da girare):**
  - Intensità del riflesso (opacità di picco): bordi scatole `0.50`, separatori `0.60` (in `globals.css` e `apple-foundation.css`).
  - Ampiezza dell'alone: bordi sfumano al `58%`; separatori ~`130px` lungo la linea.
  - Opacità delle linee separatrici: colonne/KPI `22%`, divisore di sezione `20%`. Spessore `1px`.
- **Bussola di design (leggere prima di lavorare sull'aspetto):** `archivio-documenti/design-linguaggio-apple-web.md`.
- **Errori operativi da evitare (incl. anteprima e i due tipi di "card"):** `archivio-documenti/note-operative-ai.md`.
- **Contratto di lavoro condiviso:** `CLAUDE.md` · **Avvio e comandi:** `installazione-e-avvio.md`.
