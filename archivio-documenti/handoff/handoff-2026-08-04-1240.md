# Handoff — staffetta del 4 agosto 2026, ore 12:40

**Scritto da:** Jacopo (settimana)
**Per chi riprende:** la prossima sessione (Jacopo, o Claudio nel weekend)

## Cosa ho fatto

Cinque commit, tutti **pushati su `main`**: `8158982`, `ea1ec14`, `56c018f`, `c827ebc`, `b04e5f4`.

**1. Chiuso il difetto gemello della pagina Memo** (commit `8158982`) — era il lavoro lasciato in sospeso ieri sera. Creando un memo la selezione saltava sul primo invece di restare sul nuovo, per lo stesso motivo già corretto nelle impostazioni pipeline: la funzione che ricarica l'elenco (`refetch`) dichiarava di aver finito **prima** che i dati fossero arrivati. Ora i quattro elenchi del modulo checklist (`src/modules/checklists/hooks/useChecklistsQueries.js`) poggiano su un unico pezzo di codice condiviso con lo stesso contratto del modello pipeline. Sette test nuovi accanto al file. Verificato dal vivo sulla pagina: creazione, archiviazione ed eliminazione si comportano tutte come devono, e il workspace di prova è stato ripulito.

**2. Suite di test riparata alla radice** (commit `ea1ec14`). Il problema che ieri sembrava "un rosso non identificato" era la macchina: la suite avviava **un processo separato per ogni file di test**, e sotto carico quei processi non partivano proprio (in due giri di oggi sono rimasti a terra prima 3 e poi 14 file su 28, con zero test eseguiti). Passata a **worker-thread** (`pool: 'threads'` in `vite.config.js`): primo giro completo tutto verde della giornata, **28 file / 174 test in 12 minuti** contro i 18-27 di prima. Provata anche l'opzione che riusa gli ambienti (suite giù a 109 secondi!) ma **ritirata**: mescola i finti moduli fra i file di test e produce 11 rossi finti.

**3. Regole di metodo aggiornate** (commit `56c018f` e `c827ebc`), dopo un'analisi dell'agent architetto su "perché le procedure di riordino sono così lente". La risposta, coi numeri del registro: il team di agent pesa il 10-13% di un giro e rende più di quanto costa; il collo di bottiglia è la macchina. Cosa è cambiato:
- **In CLAUDE.md**: durante il lavoro si lanciano solo i test della cartella toccata, la suite intera **una volta sola** prima della revisione finale e **in background**.
- **Spezzatura dei file grossi in un giro solo** (sotto le ~1.000 righe): l'esploratore ha una sezione nuova nel suo file di istruzioni e consegna subito il **piano completo di estrazione** (quali blocchi, in che ordine, con quali confini e quali test); si committa un pezzo alla volta dentro lo stesso giro, così se la sessione finisce non si perde nulla; **una sola revisione**, a giro completo, per le rifattorizzazioni che non toccano database, permessi o funzioni AI.
- **Mappa del progetto sempre fresca**: un hook di git (`.githooks/pre-commit`) la rigenera a ogni commit, dura meno di un secondo e non blocca mai. Su questo computer è già attivo; **su un altro clone va acceso una volta sola** con `git config core.hooksPath .githooks`.

**4. Il monitor dei consumi ora dice anche la velocità** (commit `b04e5f4`): unità al minuto, nel quadro di `npm run consumi` e come colonna nuova del registro per compito, con le dieci righe già presenti ricalcolate nello stesso passaggio. Serve a rispondere alla domanda "questo modo di lavorare ci sta dentro la finestra di 5 ore?". **Non serve a giudicare gli agent** — per quelli restano il bilancio costi/risparmi e il confronto fra lavori simili: è scritto sia nel registro sia nelle istruzioni dell'architetto, perché è l'errore più facile da fare.

**5. Esclusione antivirus della cartella di progetto**: applicata a mano da Jacopo (Windows Defender, cartella `C:\Users\jacop\Documents\crmadv`). L'effetto **non è ancora misurato**.

## Dove mi sono fermato

- **Tutto committato e pushato**, tranne questo handoff e i due documenti che vanno nel commit di chiusura insieme a lui (registro dei compiti e note operative).
- **Dev server: tutti spenti.** Porte 4000 e 5173 verificate libere.
- **Nessuna migrazione database** in questa sessione. Resta, di vecchia data, l'arretrato dell'area Agency (`20260706085001`) da riconciliare per Claudio.
- **Suite frontend: 174 test su 28 file, ultimo giro completo tutto verde.** Attenzione a un punto: anche coi worker-thread, se la macchina è molto carica qualche file può fallire **in avvio** (messaggio "Failed to start threads worker", zero test eseguiti). Non è codice rotto: si rilancia mirato quel file. Solo un fallimento di *asserzione* è un difetto vero.
- **Da sapere prima di toccare la pagina Memo**: dove il codice ricarica due elenchi di fila dopo un salvataggio, ora le due ricariche avvengono **in sequenza** e non più in parallelo. In locale non si nota; su un server remoto il messaggio di conferma arriva un po' più tardi. È lo stesso prezzo già pagato dalla pagina impostazioni pipeline, ed è accettato consapevolmente.
- **Effetto dell'esclusione antivirus: da valutare sui giri futuri**, non con una prova dedicata. Questa macchina oscilla di circa il 50% fra giri identici, quindi un singolo numero non distinguerebbe l'effetto dal rumore; i giri di test delle prossime sessioni sono già i punti di misura. Se col tempo risulterà ininfluente, l'esclusione si toglie con `Remove-MpPreference -ExclusionPath "C:\Users\jacop\Documents\crmadv"` da un PowerShell come amministratore. Il confronto è con i numeri di oggi: 12 minuti la suite completa, di cui circa 7 solo per preparare gli ambienti di test.
- **Punti dell'handoff di ieri: entrambi chiusi** (suite rilanciata e verde, difetto gemello Memo corretto). La sessione di re-naming/demo resta in standby fino a fine riordino, come prima.

## Come riprendere — prossimi passi

1. **Prossimo file grosso da spezzare: il blocco Agency**, in quest'ordine — `src/views/Agency/project/AgencyProjectWebPage.jsx` (890 righe), poi `AgencyProjectAdsPage.jsx` (993), poi `AgencyProjectAssetsPage.jsx` (1.214). Sono tre pagine sorelle della stessa area: molto probabilmente ripetono la stessa impalcatura, quindi il primo giro trova i pezzi comuni e gli altri due li riusano (è quello che ha reso il giro 2 di pipeline il più produttivo del registro). Si parte dalla più piccola apposta: si impara lo schema dove si rischia meno. Ne beneficia anche `AgencySettingsPage.jsx` (739).
2. **Il metodo è quello nuovo**: `npm run mappa` → **esploratore in modalità piano di estrazione** → si estrae tutto in un giro solo, committando un pezzo alla volta → verifica nell'anteprima → **revisore una volta sola**, a giro completo → commit di chiusura.
3. **Restano in lista, dopo il blocco Agency**: `src/views/Authentication/SignUp/Signup/index.jsx` (920 righe, area registrazione: piedi di piombo), `src/views/Checklists/ChecklistTemplates.jsx` (877), `src/views/Calendar/index.jsx` (807) e tredici file fra 500 e 800 righe. **`src/views/WebAssets/index.jsx` (2.706) resta per ultimo**, come già deciso.
4. In chiusura di sessione: `npm run consumi:compito -- "<nome>"` per ogni pezzo di lavoro chiuso, e spegnere i server. È parte del metodo.

## Riferimenti utili

- `npm run test:frontend` — la suite del frontend (28 file, 174 test). Da lanciare **in background**, una volta sola prima della revisione.
- `npm run test:scripts` — i test degli script del monitor consumi (51), pochi secondi, senza database.
- `npm run mappa` — rigenera la fotografia del progetto; ora lo fa anche l'hook a ogni commit.
- `npm run consumi` — quadro dei consumi, ora con la velocità in unità al minuto.
- `archivio-documenti/note-operative-ai.md` — nota **#37** aggiornata oggi (worker che non partono, cosa fare; stato dell'esclusione antivirus).
- `archivio-documenti/team-agenti.md` — registro delle decisioni, con le sei righe di oggi: opzioni adottate, sciame di agent non adottato e a quali condizioni riaprire il discorso, e come vanno letti i due modi di misurare i consumi.
- Pagina Memo: `/checklists/templates`. Accesso di prova `giulia.ferrari@demo.local` / `demo123`.
