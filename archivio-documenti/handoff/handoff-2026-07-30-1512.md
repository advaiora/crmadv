# Handoff — staffetta del 30 luglio 2026, ore 15:12

**Scritto da:** Jacopo (settimana)
**Per chi riprende:** la prossima sessione (Jacopo in settimana o Claudio nel weekend)

> Sessione **separata dalla linea demo**: qui si è lavorato solo a valutare uno strumento esterno e a migliorare il "team di agent". La linea demo è già chiusa e committata (handoff 1157).

## Cosa ho fatto

1. **Valutato lo strumento esterno "Graphify" → deciso di NON adottarlo.** Promette di trasformare la codebase in un "grafo" navigabile per far leggere meno file all'AI. Verificata sicurezza e legittimità (repo reale e molto diffuso, pacchetto con provenienza verificata): è legittimo. Ma non conviene a noi, per due motivi concreti: (a) carica le informazioni nel **contesto principale** — dove si paga la rilettura a ogni turno — invece di isolarle come fanno i nostri agenti; (b) il suo lettore **salta i file `.jsx`**, cioè quasi tutto il nostro frontend. Motivi completi nel registro di `team-agenti.md`.

2. **Riparato il misuratore dei consumi** (`scripts/agenti/consumi.mjs`). Leggeva solo i log del primo livello e non vedeva le chiamate degli agenti (che Claude Code salva in sottocartelle `subagents/`): riportava "0% agenti", un dato **falso**. Ora legge ricorsivamente — la quota reale è ~4,3%. Lezione annotata come **nota operativa #36**.

3. **Ri-eseguito l'architetto** sui numeri corretti: conferma che esploratore e revisore **si ripagano** (fanno le letture pesanti isolando il costo dalla sessione principale).

4. **Creato lo strumento "mappa del progetto"** (`scripts/agenti/mappa.mjs`, si lancia con `npm run mappa`). Genera `archivio-documenti/mappa/mappa-progetto.md`: elenca — **senza aprire i file enormi** — i moduli backend con le funzioni esportate, la catena dei permessi (segnalando i permessi del backend che non risultano nel frontend), i "centralini" da tenere allineati, i modelli del database, l'indice delle sezioni dei documenti grossi e la lista dei file da non aprire interi. È l'idea utile di Graphify fatta in casa, senza i suoi difetti. **Non è committata**: è un file generato (messo in `.gitignore`), si rigenera in meno di un secondo.

5. **Agganciato esploratore e revisore alla mappa** (la leggono come punto di partenza) e aggiunta in `CLAUDE.md` la regola: **prima di chiamarli, rigenerare la mappa** con `npm run mappa` (è uno script, costo di token nullo).

## Dove mi sono fermato

- Il lavoro è **completo e revisionato** (revisore automatico: ok). Lo chiudo con **un commit+push dedicato** che include tutte le modifiche e questo handoff. File toccati: `scripts/agenti/consumi.mjs`, nuovo `scripts/agenti/mappa.mjs`, `.claude/agents/esploratore.md`, `.claude/agents/revisore.md`, `CLAUDE.md`, `.gitignore`, `package.json` (nuovo script `mappa`), `archivio-documenti/note-operative-ai.md` (#36), `archivio-documenti/team-agenti.md` (registro §6).
- **Nessuna migrazione** (solo strumenti e documenti): niente da riconciliare col database.
- **Nessun dev server avviato** in questa sessione: porte 4000 e 5173 **libere**.
- **C'è una seconda sessione in standby** (in un'altra finestra): ha già letto il suo handoff (il **1157**, sul re-naming delle aree + test demo) ed è in attesa. Vale la regola "uno alla volta": non farla editare mentre si lavora su altro.

## Come riprendere — prossimi passi

1. **Sessione dedicata al frontend `.jsx`** — prossimo lavoro deciso, **da fare prima** del re-naming (scelta di Jacopo). Il frontend è l'area più fragile: è in JS/JSX **senza tipi**, **senza test**, fuori dai controlli automatici (il `tsconfig` copre solo `server` e `prisma`), e con file enormi (es. `src/views/WebAssets/index.jsx` 2.706 righe, `src/views/Agency/chat/AiChatWidget.jsx` 1.454). Obiettivo: metterlo in ordine **e** definire **regole di manutenzione** che evitino di riaccumulare lo stesso debito. Nota: la "mappa del progetto" per ora dettaglia bene il backend ma **non** i file `.jsx` — estenderla al frontend può essere parte di questa sessione.
2. **Poi la sessione re-naming/demo** (in standby, handoff **1157**): rinominare le aree dai nomi poco chiari ("Agency", "Discovery") e testare i clienti demo. Da fare **dopo** la #1, una alla volta.

## Riferimenti utili

- `npm run mappa` (→ `scripts/agenti/mappa.mjs`) — genera la mappa del progetto; **rigenerala prima di chiamare esploratore/revisore**.
- `node scripts/agenti/consumi.mjs` — monitor consumi (ora conta anche gli agenti).
- `archivio-documenti/team-agenti.md`, registro §6 — le decisioni di oggi (Graphify non adottato, fix misuratore, mappa).
- `archivio-documenti/note-operative-ai.md` #36 — perché il misuratore deve entrare nelle sottocartelle `subagents/`.
