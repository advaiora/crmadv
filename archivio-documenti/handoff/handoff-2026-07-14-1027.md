# Handoff — staffetta del 14 luglio 2026, ore 10:27

**Scritto da:** Jacopo
**Per chi riprende:** con ogni probabilità ancora Jacopo. Se ci mette mano **Claudio** nel weekend: la nota sul database qui sotto è per te — c'è **una migrazione nuova**.

---

## ⚠️ DATABASE — LEGGERE PRIMA DI TUTTO

In questa sessione ho aggiunto **una nuova migrazione**: `20260713144744_project_chat` (tabella `ProjectChatMessage`, per la chat AI di progetto). Come sempre va applicata **dopo** l'arretrato `20260706085001`.

**Come allinearsi (invariato):**
- In sviluppo, potendo perdere i dati locali: `npx prisma migrate reset` poi `npm run db:seed:demo`.
- Conservando i dati: prima `npx prisma migrate resolve --applied 20260706085001` (se non già fatto), poi `npx prisma migrate deploy`.

Sul mio ambiente è già applicata e il client Prisma è rigenerato. Promemoria: password demo `admin123`; nel `.env` serve `PLATFORM_ADMIN_EMAILS="superadmin@demo.local"`; **manca la chiave OpenAI** → l'AI risulta "non configurata" e le funzioni che generano davvero (chat, RAG) non producono output finché non si configura una chiave in Impostazioni Agency.

## Cosa ho fatto

Tutto committato e pushato su `main`. Quattro blocchi, tutti verificati (TypeScript senza nuovi errori — resta fermo a 233 preesistenti; endpoint provati via chiamate reali; frontend compila e lint pulito).

**1) Chat AI di progetto (per-utente) — funzionante.**
Nuova scheda **"Chat"** dentro ogni progetto Agency (barra schede in cima al progetto, tra Discovery e Web). L'utente fa domande e l'AI risponde usando **solo** le Fonti indicizzate del progetto (ricerca semantica RAG già esistente), citando le fonti usate e dichiarando quando l'informazione non c'è. La conversazione è **salvata** ed è **privata per utente**. Rispetta il budget AI giornaliero e finisce nel log costi. File nuovi principali: `server/repositories/project-chat.repository.ts`, `src/views/Agency/project/AgencyProjectChatPage.jsx`; nel motore AI ho aggiunto un percorso di generazione a **testo** (accanto a quello che produce JSON).

**2) Rendiconto consumi AI per-workspace — in Impostazioni Agency.**
Nuovo pannello **"Consumi & costi AI"** dentro Impostazioni Agency (accanto al Budget): spesa per dipendente, per funzione, ultime chiamate, con filtri. In parallelo la **Console piattaforma** è stata **sfoltita**: ora mostra solo la panoramica cross-workspace (totali + per-workspace + config), mentre il dettaglio per-utente vive in Agency. Motivo: il dettaglio consumi ha senso per il singolo workspace, non solo per il super-admin di piattaforma. Nessuna migrazione (sola lettura del log esistente). File nuovi: `src/views/Agency/AgencyAiUsagePanel.jsx`.

**3) Stima costo sui pulsanti AI.**
Accanto ai pulsanti AI principali (Discovery brief e sezione, Web "Genera con AI", Ads "Rigenera asset AI") compare un **range di costo stimato**. All'inizio è indicativo; con l'uso si affina sui consumi storici reali di quella funzione, col modello attualmente configurato. Nessuna migrazione. File nuovi: `src/views/Agency/AiCostEstimate.jsx`, `src/modules/agency-os/hooks/`. Non compare se l'AI non è configurata (in quel caso la generazione è rule-based e gratuita).

**4) Chat AI collaborativa — SPEC e PIANO su carta (NON sviluppata).**
Con Jacopo abbiamo progettato l'evoluzione della chat in una **Chat AI collaborativa** (multi-utente su invito, ambiti Generale/Cliente/Progetto, popup globale in tutto il CRM, allegati, tempo reale via websocket, compressione del contesto, navigazione suggerita). È tutto **scritto**, niente ancora sviluppato. Documento: `archivio-documenti/spec-chat-ai-collaborativa.md` (spec + piano a 6 fasi). Aggiornata anche la roadmap.

## Dove mi sono fermato

- **Tutto committato e pushato, albero pulito.**
- **La chat collaborativa NON è iniziata:** esiste solo la spec su carta. Il codice in `main` ha ancora la chat **per-utente** (base). La versione condivisa/multi-ambito è la Fase 1 del piano.
- **Imperfezioni grafiche note (da sistemare):** nell'area Agency (schede progetto, es. Discovery) ci sono box che restano **bianchi in tema scuro** e scritte poco leggibili su alcuni pulsanti AI. È debito grafico in parte ereditato dal template, in parte nelle mie aggiunte recenti (il badge costo usa `bg="light"`). Va fatta una **passata di pulizia grafica** (chiaro/scuro) — registrata in roadmap come rifinitura di fine V4.
- **Le parti AI si collaudano davvero solo con le chiavi reali:** senza chiave OpenAI/Anthropic nel `.env`, chat e RAG girano "a vuoto". La verifica end-to-end vera delle funzioni AI è rimandata a quando si configurano le chiavi (previsto a fine di questa V).

## Come riprendere — prossimi passi

1. **`git pull`** per avere gli ultimi commit.
2. **Allinea il database** (vedi avviso in cima): applica la migrazione `20260713144744_project_chat`.
3. Avvio (due terminali): `npm run dev:api` (porta 4000) e `npm run dev` (porta 5173). Login: `admin@test.com / admin123`. La Chat è dentro un progetto: Agency → Progetti → apri un progetto → scheda **Chat**.
4. **Rifinitura consigliata prima di aprire la chat collaborativa:** l'**audit grafico** dell'area Agency (chiaro/scuro), contro `archivio-documenti/design-linguaggio-apple-web.md` e con `npm run lint:colors`.
5. **Quando si parte con la Chat collaborativa:** seguire il piano a fasi in `spec-chat-ai-collaborativa.md`, a partire dalla **Fase 1** (modello conversazioni + chat di progetto condivisa su invito). Attenzione: la Fase 2 dipende dall'assegnazione/visibilità di **V2** (area con possibile lavoro di Claudio) → verificare prima di muovere.
6. **A fine V:** configurare le **chiavi reali OpenAI/Anthropic** e preparare una lista di test per collaudare tutto il lavoro AI.

## Riferimenti utili

- **Spec Chat collaborativa (fonte di verità della feature):** `archivio-documenti/spec-chat-ai-collaborativa.md`.
- **Roadmap aggiornata:** `archivio-documenti/03-roadmap-confronto-e-build.md` (blocco V4: chat base fatta + estensione pianificata; nota audit grafico; V10: onboarding esteso a tutto il CRM).
- **Chat di progetto (base):** `server/repositories/project-chat.repository.ts`, motore in `server/modules/agency-os/agency.service.ts`, UI `src/views/Agency/project/AgencyProjectChatPage.jsx`.
- **Rendiconto consumi:** `src/views/Agency/AgencyAiUsagePanel.jsx`; **stime costo:** `src/views/Agency/AiCostEstimate.jsx`.
