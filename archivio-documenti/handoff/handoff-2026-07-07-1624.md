# Handoff — staffetta del 7 luglio 2026, ore 16:24

**Scritto da:** Jacopo
**Per chi riprende:** la prossima sessione (Claudio nel weekend, o Jacopo nei feriali)

---

## ⚠️ AVVISO ANCORA APERTO PER CLAUDIO — database (riportato dal precedente handoff)

Questo punto **non è ancora stato affrontato** (Claudio non ha ancora ripreso il CRM): resta valido.

La migrazione `20260706085001_user_theme_avatar_preferences` (già su `main`) ha **impacchettato tutto l'arretrato dell'area Agency** accumulato da inizio marzo con `db push` senza migrazioni (tabelle ProjectType, ProjectMemory, ProjectAlert, ecc.). Se applicata su un database che **ha già** quelle tabelle, va in errore *"relation already exists"*.

**Prima di applicarla, riconcilia lo stato**:
- se il tuo DB è già allineato allo schema: `npx prisma migrate resolve --applied 20260706085001` (segna la migrazione come applicata senza rieseguirla);
- oppure, in sviluppo, se puoi perdere i dati locali: `npx prisma migrate reset`.

**Da decidere nel team:** usiamo le *migrazioni* Prisma o `db push`? Da marzo di fatto si è usato `db push`. Meglio un metodo unico.

---

## ⚠️ NOTA IMPORTANTE: i commit di questa sessione potrebbero NON essere ancora su remoto

Ho fatto **4 commit su `main`** in questa sessione. Verifica con `git log` / `git status` se sono stati **pushati**: se non lo sono, fai `git push` prima di lavorare, altrimenti chi riprende non li vede. (Se leggi questo file dal repo remoto, allora il push è avvenuto.)

---

## Cosa ho fatto

Sessione lunga, tutta sul **design "a sottrazione" e la personalizzazione dei colori**. Quattro commit:

1. **Branding a palette con guardrail di leggibilità** (commit `d000a4c`). Prima il workspace aveva due color-picker liberi, senza vincoli: un colore sbagliato rendeva link/bottoni illeggibili. Ora:
   - una **palette curata di 13 tinte** (Blu, Indaco, Viola, Rosa, Rosso, Arancio, Ambra, Verde, Menta, Teal, Ciano, Grafite, Marrone), ognuna **pre-validata** per restare leggibile (contrasto minimo) in chiaro **e** in scuro;
   - in tema scuro l'accento usa una **variante desaturata** (prima applicava lo stesso colore a entrambi i temi);
   - al caricamento del **logo**, l'app ne estrae il colore dominante e **suggerisce la tinta della palette più vicina**;
   - rimossi i due color-picker liberi. Nessuna modifica al database.
   - File: `src/lib/brandingPalette.ts`, `src/lib/logoColor.js`, `src/lib/workspaceBranding.ts`, `src/views/WorkspaceBranding/index.jsx`.

2. **Divulgazione progressiva "a linguetta" sui Clienti** (commit `2e64765`). Pilota del pattern Apple-style: la lista resta densa (nome, tipo, email, telefono, tag sempre visibili) ma ogni riga si **espande** con un chevron per rivelare i campi secondari (P.IVA, Codice Fiscale, indirizzo, note) senza cambiare pagina. Per farlo la lista Clienti è stata convertita da tabella a **griglia di div** e le righe **memoizzate** (per non far scattare l'interfaccia all'apertura). File nuovi: `src/components/ui/CollapsibleSection.jsx`, `src/modules/clients/ui/components/ClientRowDetails.jsx`. Regola scritta nel design doc §3.4.

3. **Animazione della linguetta resa fluida** (commit `656c917`). L'animazione scattava perché animare l'altezza su questo template pesante costringeva il browser a ridisegnare troppo. Ora la primitiva `CollapsibleSection` anima via **transform** (sul compositore/GPU): lo spazio si apre e il contenuto scorre dentro, tutto fluido. File: `src/components/ui/CollapsibleSection.jsx`.

4. **Stessa linguetta estesa ai Preventivi** (commit `72549aa`). La riga preventivo si espande mostrando **subtotale, imposte (con aliquota), emissione, validità, note** — dati già presenti nella risposta di lista, solo non mostrati. Creati i **componenti riusabili** per le prossime viste: `src/components/ui/RowDisclosureButton.jsx`, `src/components/ui/DetailField.jsx`, e le classi CSS generiche `.ui-row-*` / `.ui-detail-*` in `globals.css`.

## Dove mi sono fermato

- **Tutto committato, albero pulito.** Ultimo commit: `72549aa`.
- **Verificato in anteprima** dove c'erano dati demo: branding (palette, anteprima chiaro/scuro, suggerimento dal logo) e la linguetta su Clienti e Preventivi funzionano.
- **La "linguetta" è stata volutamente NON estesa a tutto il CRM.** Verificando i modelli reali:
  - **Team, Vault**: niente campi secondari veri (modello minimale / solo segreti cifrati) → esclusi;
  - **Web Assets**: la vista è già abbastanza pulita → lasciata fuori;
  - **Agency Projects**: valore marginale (in lista arrivano solo descrizione-tipo, data creazione e moduli; **manca** il riepilogo `contextSummary`, che è solo nel dettaglio). In più l'Agency-OS è escluso dai dati demo di proposito. Deciso di **lasciarlo fuori**.
- Il pattern e i pezzi riusabili restano pronti: applicarlo a una nuova vista-lista è questione di pochi minuti (vedi come è fatto in Preventivi).

## Come riprendere — prossimi passi

1. **`git pull`** (o, se i commit non risultano su remoto, prima `git push` da questa macchina — vedi nota sopra).
2. **Allinea il DB** se serve (vedi avviso in cima se sei Claudio). Avvio: `npm run dev:api` (porta 4000) + `npm run dev` (5173). Login di prova: **admin@test.com / admin123**.
3. **Prosegui la roadmap.** La parte "brandizzazione colori" della V1 è chiusa; il pattern di divulgazione progressiva è pilotato e documentato. Punti V1 sostanzialmente coperti: valutare cosa aprire della **V2** (Super Admin console, ruoli "Discord-style", reparti) o completare rifiniture V1.
4. Quando tocchi stili: `npm run lint:css` e `npm run lint:colors` (0 errori attesi; restano 11 avvisi preesistenti in `clients-ui.css`, non nostri). I file `.ts` non si lintano con ESLint del progetto: si controllano con `tsc` (vedi `note-operative-ai.md` §6). Le viste con doppio render tabella/card: attento che in anteprima stretta la tabella desktop è nascosta e misura 0 (note §7).

## Riferimenti utili

- **Branding palette:** `src/lib/brandingPalette.ts` (+ helper); applicazione temi in `src/lib/workspaceBranding.ts`; pagina in `src/views/WorkspaceBranding/index.jsx`.
- **Pattern "linguetta" (divulgazione progressiva):** primitiva `src/components/ui/CollapsibleSection.jsx`; pezzi riusabili `RowDisclosureButton.jsx` + `DetailField.jsx`; CSS generico `.ui-row-*` in `src/styles/scss/globals.css`. Regola di design in `archivio-documenti/design-linguaggio-apple-web.md` §3.4. Esempi: Clienti (`src/views/Clients/ClientsList.jsx`) e Preventivi (`src/views/Quotes/QuotesList.jsx`).
- **Errori operativi da evitare (aggiornati):** `archivio-documenti/note-operative-ai.md` (nuove voci §6-9: lint TS, anteprima stretta, animazioni tabella vs griglia, re-render React, backdrop-filter).
- **Roadmap (dove siamo: V1 quasi chiusa):** `archivio-documenti/03-roadmap-confronto-e-build.md`.
