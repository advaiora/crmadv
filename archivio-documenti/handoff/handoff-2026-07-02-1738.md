# Handoff — staffetta del 2 luglio 2026, ore 17:38

**Scritto da:** Jacopo
**Per chi riprende:** Claudio

## Cosa ho fatto

Sessione lunga, tutta su **aspetto Apple-style**. Tutto committato e pushato su `main` (ultimo commit `2b711c8`). In ordine:

1. **Chiuso il Task 2 della V1** (rifinitura Apple dei componenti): ho portato ombre, raggi e transizioni scritti "a mano" sui **token** del design system (le variabili `--shadow-*`, `--radius-*`, `--duration-*`) in `src/styles/scss/globals.css`. Riguarda card, dropdown, modali, tabelle, bottoni (con ombra tenue sul primario) e la barra in alto (topnav), che ora è leggermente translucida con effetto "vetro smerigliato". Anche i raggi residui (chip workspace, avatar, link footer, voce sidebar) sono su token.

2. **Sistemato un intoppo dell'anteprima locale.** Con un'altra sessione già attiva sulla porta 5173, l'anteprima puntava alla porta sbagliata. Ho fatto in modo che l'avvio usi la porta assegnata quando serve (`vite.config.js`) e ho aggiunto `autoPort` in `.claude/launch.json`. L'avvio normale (`npm run dev`) resta identico.

3. **Creato il documento fondativo di design** `archivio-documenti/design-linguaggio-apple-web.md`: è la **bussola** del progetto per l'aspetto Apple-style (principi, regole "fai/non fare", valori concreti agganciati ai token, checklist per pagina). Nasce da una ricerca su più fonti (linee guida Apple + fonti autorevoli di web design). L'ho collegato in `CLAUDE.md` e in `design-system-temi.md` così si trova sempre. **Da leggere prima di lavorare sull'aspetto.**

4. **Prototipo Apple sulla Dashboard.** Ho ridisegnato: intestazione senza il "riquadro" attorno al titolo (titolo grande e in grassetto), tessere KPI con numero grande, icona in un cerchio colorato tenue e griglia compatta, intestazioni di sezione in stile iOS. Poi, su richiesta, ho introdotto un **bordo "vetro" discontinuo** sui blocchi: una classe riutilizzabile `.glass-edge` (in `globals.css`) che disegna un contorno che si accende agli angoli e **si apre lungo i lati** (non chiude mai il perimetro), con un tenue riflesso di luce. Adattivo a chiaro/scuro. Questo effetto è stato **ufficializzato nel documento** (§6.5) come stile della casa per i blocchi.

5. **Esteso il bordo "vetro" a tutta l'app dove pulito:** l'ho reso il default del componente card React (`src/components/ui/card.jsx`), quindi lo ereditano Dashboard, Impostazioni e Checklist. **Non** l'ho messo sulle card del template Bootstrap/Jampack perché lì collide con le classi-bordo del template (es. `.card-border`) e farebbe doppio bordo.

## Dove mi sono fermato

- **Tutto stabile e pushato** su `main` (ultimo commit `2b711c8`), albero pulito. App funzionante, verificata in chiaro e scuro.
- **Grande tema aperto: rivedere l'aspetto del CRM "dalle fondamenta".** Jacopo ha chiesto di ripensare il vestito dell'app, perché i riquadri Bootstrap/Jampack non sono in linea con lo stile Apple. **Ho solo studiato e preparato il piano — non ho implementato niente**, perché prima serve una decisione da prendere **insieme a te (Claudio)**.
  - **Scoperta chiave dello studio:** le fondamenta sono **già circa all'80% "Apple"** — colori, tema chiaro/scuro, tipografia, spazi, raggi, ombre, motion, e anche bottoni/form/tabelle sono già gestiti dai nostri token e "vincono" sul template. Quello che resta "Bootstrap" sono soprattutto le **superfici (card)** e qualche controllo minore, più la collisione `.card-border`.
  - **Nota su "Bootstrap" (per evitare fraintendimenti):** Bootstrap non è solo i blocchi/riquadri — è il kit di componenti pronti usato in TUTTA l'app (anche menu, finestre, griglie). Quindi non serve rimuoverlo tutto e subito (~121 file: rischio enorme, tocca anche cose che funzionano). Per i **blocchi** il fine ultimo è comunque uscirne, ma progressivamente. Strada raccomandata: **(a)** un **layer di fondazione CSS** (un file nuovo `src/styles/scss/apple-foundation.css`) che da un punto solo dà a tutta l'app l'ASPETTO Apple e risolve la collisione `.card-border` in modo pulito (specificità controllata, senza `!important` a raffica); **(b)** poi, in modo progressivo, sostituire la **struttura** dei blocchi con le primitive pulite `src/components/ui/*` (la Dashboard è il modello). Importante: gran parte della sensazione "scatolosa" è il *vestito*, non la struttura — quindi se serva anche ristrutturare i blocchi si decide **dopo** aver visto il ri-vestito (Fase 1).
  - Il piano dettagliato è salvato sul mio computer in `C:\Users\jacop\.claude\plans\typed-singing-wirth.md` (potrebbe non esserti accessibile): i punti essenziali sono qui sotto e nel documento di design.

## ⚠️ Tre domande aperte — da decidere insieme prima di procedere

Jacopo vuole confrontarsi con Claudio. **Alla prossima sessione, dopo la sintesi qui sopra, riproporre queste tre opzioni di scope** (fin dove spingere la prima tornata sul nuovo aspetto):

1. **Solo layer di fondazione (Fase 1)** — un unico file CSS che dà a tutta l'app il linguaggio Apple (bordo vetro di default sulle card, collisione `.card-border` risolta, piccoli buchi colmati). App-wide, reversibile, nessun file JSX toccato. *(È la mia raccomandazione come primo passo.)*
2. **Fondazione + 1 pagina pilota** — Fase 1 + cominciare a rifare UNA pagina legacy ad alto traffico (es. Clienti) sostituendo i mattoni Bootstrap con le primitive pulite `ui/`, per vedere l'aspetto "finale".
3. **Spingi su più pagine** — Fase 1 + migrazione delle pagine principali (Clienti, Progetti, Preventivi) alle primitive `ui/`. Ampio, su più sessioni.

## Come riprendere — prossimi passi

1. **Leggi questo handoff** e poi il documento di design `archivio-documenti/design-linguaggio-apple-web.md` (la bussola).
2. **Decidi con Jacopo quale delle tre opzioni** qui sopra adottare per il nuovo aspetto.
3. **Avvia l'app** (due terminali nella cartella del progetto): se serve `npm install`; poi `npm run dev:api` (backend, porta 4000) e `npm run dev` (frontend, porta 5173). Login di prova: **admin@test.com / admin123**. Nota: al primo avvio dopo un `npm install` la pagina resta vuota ~10-30 s (Vite ri-ottimizza), è normale, **aspetta senza ricaricare**.
4. **Se si sceglie la Fase 1:** creare `src/styles/scss/apple-foundation.css` e importarlo in `src/main.jsx` **dopo `globals.css` e prima di `design-tokens.css`**. Usare come prefisso `.hk-wrapper` + `:where()` per battere il template in modo pulito; neutralizzare la collisione una volta sola con `.hk-wrapper .card.card-border { border: 0 }` così resta solo l'anello vetro; rendere il bordo vetro il default delle card con opt-out `.no-glass` (e una variante piatta per liste dense). Verificare su Dashboard, Clienti ed Email in chiaro e scuro. Dettagli completi nel piano.
5. Quando tocchi stili di moduli, lancia `npm run lint:css` e `npm run lint:colors` per non reintrodurre colori scritti a mano.
6. A fine sessione, genera il nuovo handoff con `/handoff`.

## Riferimenti utili

- **Bussola di design (leggere prima):** `archivio-documenti/design-linguaggio-apple-web.md`
- Regole colori/token e tema: `archivio-documenti/design-system-temi.md`
- Errori operativi da evitare (incl. lentezza anteprima + porta anteprima): `archivio-documenti/note-operative-ai.md`
- Roadmap e debito tecnico: `archivio-documenti/03-roadmap-confronto-e-build.md`
- Colori/tema e classe `.glass-edge`: `src/styles/scss/globals.css`; token in `src/styles/design-tokens.css`; branding a runtime `src/lib/workspaceBranding.ts`
- Modello di card "pulita" (per la migrazione futura): `src/components/ui/card.jsx`
- Contratto di lavoro condiviso: `CLAUDE.md` · Avvio e comandi: `installazione-e-avvio.md`
