# Handoff — staffetta del 3 luglio 2026, ore 17:52

**Scritto da:** Jacopo
**Per chi riprende:** la prossima sessione (Claudio nel weekend, o Jacopo nei feriali)

## Cosa ho fatto

Sessione lunga e produttiva sull'aspetto Apple-style e sul primo pezzo della V1. **Tutto è committato e pushato su `main`** (10 commit oggi, l'albero di lavoro è pulito): si può ripartire senza preoccuparsi di salvataggi in sospeso.

1. **Effetto vetro reso fluido su tutta la pagina.** Prima il riflesso che segue il mouse sui bordi delle scatole si accendeva su un blocco solo per volta, con uno stacco netto quando il cursore passava a quello accanto. Ora è un "campo di luce" continuo: più blocchi si illuminano insieme, in proporzione alla vicinanza del cursore, e il passaggio è morbido. File: `src/components/effects/GlassPointer.jsx`. C'è una manopola di calibrazione (`GLOW_RADIUS`, in cima al file) per l'ampiezza dell'alone. **Approvato dal vivo.**

2. **Layout "senza scatole" (stile iOS Impostazioni/Mail) trasformato in un sistema riutilizzabile ed esteso a più pagine.** Il test che c'era solo sulla Dashboard è diventato un set di classi generiche in `src/styles/scss/apple-foundation.css`:
   - `page-flat` sul contenitore di pagina → i blocchi perdono la scatola;
   - `flat-cols` / `flat-cols-tight` / `flat-rows` / `flat-list` → per disporre i separatori-vetro tra i blocchi;
   - `flat-keep` → opt-out per il singolo blocco che deve **restare** una scatola (l'"approccio misto" per le pagine dense).
   Applicato a: **Checklist**, **Impostazioni→Moduli** (lista raggruppata), e con approccio misto a **Clienti**, **Preventivi**, **Team** (intestazioni/filtri senza scatola, tabelle mantenute come scatola con l'anello vetro reattivo).

3. **Sistemato il menu laterale a tendina** (4 problemi segnalati da te):
   - Leggibilità delle voci **attive** in chiaro e scuro (prima testo nero su blu / bianco su celeste, illeggibili);
   - Menu in versione ridotta: ora mostra l'**icona della sezione** nel riquadro (prima riquadro blu vuoto e scritte tagliate);
   - **Evidenziazione**: prima restava accesa la prima sottovoce anche quando erano attive le altre; ora si accende quella giusta;
   - **Nomi** delle voci allineati ai titoli delle pagine (es. "Crea Preventivo" → "Nuovo Preventivo", "Lista Clienti" → "Clienti", ecc.).

4. **Creato uno script di dati demo per i test.** Comando `npm run db:seed:demo` (file `prisma/seed-demo.ts`): riempie il workspace Demo con 12 clienti, 14 preventivi in tutti gli stati, 2 template, 6 membri team e 2 inviti. Serve a valutare le pagine "piene" e per i test futuri. È ripetibile senza creare doppioni. Documentato in `installazione-e-avvio.md`.

5. **Pulizia del boilerplate del template (parte della V1).** Rimossi 22 file di codice morto (app email demo del template, funzioni finte chat/todo/email, file di servizio vuoti in root). Nessuna pagina reale li usava; l'app è stata verificata funzionante dopo la rimozione. La pagina **Messaggi vera** resta intatta.

6. **Command-K / Advanced Find — la ricerca globale della V1.** Si apre con **Ctrl/Cmd+K** (o dal pulsante "Cerca" nella barra in alto). Due parti: (a) **navigazione** verso tutte le pagine del menu, filtrate per i permessi dell'utente; (b) **ricerca** di Clienti e Preventivi per nome/dati, con risultati cliccabili che portano alla scheda. File: `src/components/command-palette/`. Verificato via ispezione: apertura, ricerca ("ross" trova cliente e preventivo), navigazione, chiusura.

## Dove mi sono fermato

- **Tutto committato e pushato, albero pulito.** Ultimo commit: `9e74d14` (Command-K).
- **Verifica visiva rimasta in sospeso (da fare tu alla ripresa).** Durante la sessione l'anteprima locale era in secondo piano (stavo lavorando in parallelo su un'altra sessione), quindi lo strumento non riusciva a fare screenshot: gli screenshot vanno in timeout quando la finestra di anteprima è **nascosta** — è un limite dello strumento, non un problema del codice (annotato in `note-operative-ai.md`, voce 4). Ho verificato tutto ispezionando gli stili/comportamenti, ma **manca il tuo sguardo diretto** su due cose (vedi passo 2 e 3 sotto).
- **Command-K, cosa NON copre ancora:** la ricerca dei **Progetti** (la loro lista filtra per categoria, non per testo semplice: va aggiunto un parametro di ricerca dedicato prima di includerli).

## Come riprendere — prossimi passi

1. **Avvia l'app** (due terminali): `npm run dev:api` (backend, porta 4000) e `npm run dev` (frontend, porta 5173). Login di prova: **admin@test.com / admin123**. Se il database non ha ancora i dati demo, lancia una volta `npm run db:seed:demo`.

2. **Controllo in anteprima del Command-K (la ricerca globale)** — questo è il controllo che avevo lasciato a te perché dovevi chiudere:
   - Premi **Ctrl+K** (o clicca "Cerca" in alto): si apre la palette.
   - Prova a scrivere `ross`: devono comparire il cliente **Rossi Costruzioni** e il suo **preventivo**; cliccandoli si apre la scheda.
   - Prova a scrivere il nome di una pagina (es. `team`, `moduli`): deve portartici.
   - Muovi la selezione con le **frecce**, apri con **Invio**, chiudi con **Esc**.
   - **Ripeti in tema chiaro e scuro** e verifica che si legga bene.

3. **Controllo in anteprima del layout "senza scatole" con dati pieni** (l'avevi rimandato perché prima le sezioni erano vuote): guarda **Preventivi** (elenco) e **Team** — ora popolati dai dati demo — in chiaro e scuro. Giudizio: si distingue bene la parte "di contorno" (intestazioni/filtri senza scatola) dai dati densi (tabelle incorniciate)? Se qualcosa è troppo nudo o troppo incorniciato, si regola con le classi `page-flat` / `flat-keep`, senza scrivere CSS nuovo.

4. **Prosegui la V1** (in ordine ragionevole):
   - Aggiungere i **Progetti** al Command-K (serve un parametro di ricerca testuale sull'elenco progetti);
   - **Shortcuts personalizzabili** per utente;
   - **Personalizzazione utente** (tema, colori, foto profilo) cablata alle preferenze persistenti;
   - **Brandizzazione con preset + guardrail** di leggibilità in chiaro/scuro (vedi V1 nella roadmap).

5. Quando tocchi stili, lancia `npm run lint:css` e `npm run lint:colors` (0 errori attesi; restano 11 avvisi preesistenti in `clients-ui.css`, non nostri). A fine sessione genera il nuovo handoff con `/handoff`.

## Riferimenti utili

- **Command-K (ricerca globale):** `src/components/command-palette/CommandPalette.jsx` (logica) e `command-palette.css` (stile). Montato in `src/layout/MainLayout/ClassicLayout/index.jsx`; pulsante nella barra in `src/layout/Header/TopNav.jsx`.
- **Sistema layout "senza scatole":** `src/styles/scss/apple-foundation.css` (classi `page-flat`, `flat-cols`, `flat-cols-tight`, `flat-rows`, `flat-list`, `flat-keep`).
- **Effetto vetro:** `src/components/effects/GlassPointer.jsx` (manopola `GLOW_RADIUS`).
- **Menu laterale:** `src/layout/Sidebar/Sidebar.jsx` (logica evidenziazione), `SidebarMenu.jsx` (voci/nomi), regole stile in `src/styles/scss/globals.css`.
- **Dati demo:** `npm run db:seed:demo` (script `prisma/seed-demo.ts`); guida in `installazione-e-avvio.md`.
- **Bussola di design (leggere prima di lavorare sull'aspetto):** `archivio-documenti/design-linguaggio-apple-web.md`.
- **Roadmap (dove siamo: V1):** `archivio-documenti/03-roadmap-confronto-e-build.md`.
- **Errori operativi da evitare (incl. timeout anteprima e i due tipi di card):** `archivio-documenti/note-operative-ai.md`.
