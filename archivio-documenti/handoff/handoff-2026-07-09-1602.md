# Handoff — staffetta del 9 luglio 2026, ore 16:02

**Scritto da:** Jacopo
**Per chi riprende:** la prossima sessione (Claudio nel weekend, o Jacopo nei feriali)

---

## ⚠️ DATABASE — LEGGERE PRIMA DI TUTTO (soprattutto Claudio)

Se non hai ancora allineato il database in questi giorni, ci sono **migrazioni pendenti** da applicare **in quest'ordine**. Le due in fondo sono **nuove di questa sessione**:

1. `20260706085001_user_theme_avatar_preferences` — è la migrazione "problematica": impacchetta **tutto l'arretrato dell'area Agency** accumulato con `db push` senza migrazioni. Se il tuo database **ha già** quelle tabelle (ProjectType, ProjectMemory, ProjectAlert, ecc.), applicarla dà errore *"relation already exists"*.
2. `20260708074026_departments` — Reparti. Innocua (aggiunge tabelle nuove).
3. `20260708100228_project_visibility_hierarchy` — visibilità progetti. Innocua.
4. `20260708133751_platform_admin_workspace_status` — platform admin + stato workspace. Innocua.
5. `20260708151727_ai_usage_log` — tabella log costi AI. Innocua.
6. `20260709073610_ai_usage_log_user` — **NUOVA.** Aggiunge la colonna `userId` alla tabella dei log costi AI (per sapere quale utente ha fatto la chiamata). Innocua.
7. `20260709092257_custom_field_definitions` — **NUOVA.** Aggiunge la tabella `CustomFieldDefinition` (campi personalizzati) e la colonna `customFields` sui clienti. Innocua.

**Come allinearti — due strade:**
- **Strada semplice (in sviluppo, se puoi perdere i dati locali):** `npx prisma migrate reset` — cancella e ricrea il database applicando tutte le migrazioni da zero, in ordine. Poi `npm run db:seed:demo` per i dati demo.
- **Strada che conserva i dati (se il tuo DB ha già le tabelle Agency):** prima `npx prisma migrate resolve --applied 20260706085001` (segna come già applicata la migrazione problematica), poi `npx prisma migrate deploy` (applica tutte le altre).

Regola confermata in `CLAUDE.md`: **solo migrazioni tracciate (`prisma migrate dev`), mai `db push`.** Non riscrivere le migrazioni già applicate.

### ⚠️ Credenziali demo corrette
Le password di prova sono **entrambe `admin123`**: `admin@test.com / admin123` **e** `superadmin@demo.local / admin123`. (L'handoff dell'8 luglio riportava per errore `superadmin@demo.local / demo123`: era sbagliato e dava "Credenziali non valide". Ora è corretto.)

### ⚠️ Variabile d'ambiente `PLATFORM_ADMIN_EMAILS` (per la Console piattaforma)
La Console piattaforma è visibile solo ai "Super Admin di piattaforma", promossi all'avvio dalle email elencate nella variabile **`PLATFORM_ADMIN_EMAILS`** del file `.env`. Questa riga **non è nel repository** (il `.env` è ignorato da git): Claudio deve aggiungerla a mano includendo **`superadmin@demo.local`**, altrimenti nessuno viene promosso e la Console resta irraggiungibile. Esempio: `PLATFORM_ADMIN_EMAILS="superadmin@demo.local"`.

---

## Cosa ho fatto

Sessione lunga e piena. Tutto committato e pushato su `main`.

**1) Rifattorizzazione della Console piattaforma + costi AI per utente.**
- La Console piattaforma (menu → Piattaforma) è stata **divisa in due schede** dentro la pagina: "Gestione workspace & accessi" e "Consumi & costi AI". Prima era un'unica pagina troppo densa.
- I costi AI ora sono tracciati **anche per singolo utente** (prima solo per workspace): aggiunta la colonna `userId` al log delle chiamate AI, popolata automaticamente in base a chi è loggato. Nella scheda consumi ci sono i **filtri** per periodo, utente, modello e funzione, più una tabella "per utente" e "ultime chiamate".
- File: `src/views/Settings/PlatformConsole.jsx` (ora solo il guscio con le due schede) + `src/views/Settings/platform/PlatformWorkspacesPanel.jsx` e `PlatformAiUsagePanel.jsx`. Backend: nuovo `server/core/request-context.ts` (porta l'id dell'utente fino al punto in cui si salva il log), `server/repositories/ai-usage.repository.ts`, `server/services/platform-admin.service.ts`, `server/routes/admin.route.ts`. Migrazione `20260709073610`.

**2) Navigazione a schede per tutti i moduli.**
- Le **sottosezioni dei moduli non compaiono più nel menu a tendina** della barra laterale. La barra laterale mostra solo le voci principali; le sottosezioni sono ora **schede (tab) in cima alla pagina** del modulo (stesso stile della Console). Vale per tutto il CRM, **Agency compresa** (le sue 5 sottosezioni sono ora schede in alto: le pagine e le funzioni sono identiche, cambia solo dove si clicca).
- File: nuovo `src/layout/ModuleTabs/ModuleTabs.jsx` + `src/layout/Sidebar/menuUtils.js`; modificati `Sidebar.jsx`, `SidebarMenu.jsx`, `ClassicLayout/index.jsx`.
- Due ritocchi alla barra laterale: l'icona per chiudere il menu è ora una **freccia semplice** (senza barra verticale); su desktop il menu "fisso" aperto si **chiude anche cliccando fuori** da esso.
- Corretto un bug: la voce "Profilo" apriva una pagina 404 (puntava a un percorso non esistente); ora apre la scheda profilo.

**3) V3 — Campi personalizzati (Custom Fields) per i clienti.**
- Nuova funzione completa: si possono **definire campi personalizzati** per la scheda cliente (testo, numero, data, sì/no, elenco a tendina), **compilarli** nel form cliente e **vederli** nel dettaglio.
- Dove si gestiscono: menu → Clienti → scheda **"Campi personalizzati"** (in cima). Lì si creano/modificano/riordinano/eliminano.
- File backend: nuovo modulo `server/modules/custom-fields/` (definizioni + validazione dei valori) + colonna `customFields` sui clienti; validazione/salvataggio in `server/modules/clients/service.ts` e `repository.ts`. Migrazione `20260709092257`.
- File frontend: `src/modules/customFields/` (API + pagina di gestione), render nel form `src/modules/clients/ui/ClientForm.jsx` (nuova "Sezione 7"), visualizzazione in `src/views/Clients/ClientDetail.jsx`.

**4) Righe/card cliccabili in tutto il CRM.**
- Ora **si apre il dettaglio cliccando l'intera riga/card**, non solo dal menu tre puntini o da un pulsante (i menu e i pulsanti restano e funzionano: cliccarli non apre il dettaglio). Fatto con un aiutante riutilizzabile `src/utils/rowActivation.js`.
- Applicato a: elenco Clienti, elenco Progetti Agency, Template Preventivi, i 5 riquadri della Dashboard, le card Opportunità e Report di Agency. (Preventivi e la board Kanban dei Progetti erano già così.)

**5) Correzioni pagina Web Assets.**
- Il pannello di dettaglio (pulsante "View") ora si apre come **finestra modale ampia** invece che in fondo alla lista (prima non ci si accorgeva che si fosse aperto).
- Premendo di nuovo "View" sulla stessa riga, il pannello si **chiude** (prima non c'era modo di chiuderlo).
- Cliccando il nome del cliente/progetto in una riga si applica un **filtro** alla lista: ora c'è un piccolo "chip" con la ✕ per **togliere il filtro** e tornare alla lista completa.

## Dove mi sono fermato

- **Tutto committato e pushato su `main`.** Albero pulito.
- **Verifiche:** il backend è stato provato end-to-end (chiamate dirette all'API con dati di prova poi cancellati: costi AI per utente con filtri, CRUD dei campi personalizzati, validazione dei valori sui clienti). Il frontend **compila senza errori** su tutti i file toccati. La verifica *visiva a schermo* è rimasta limitata perché il pannello di anteprima non era visibile a schermo durante la sessione: conviene dare un giro veloce nel browser (vedi prossimi passi).
- **Nessun blocco lasciato a metà.** Restano dei "nice to have" già annotati in roadmap (vedi sotto).

## Come riprendere — prossimi passi

1. **`git pull`** per avere l'ultimo commit.
2. **Allinea il database** seguendo l'avviso in cima (se sei Claudio: quasi certamente ti serve) **e imposta `PLATFORM_ADMIN_EMAILS` nel tuo `.env`** con `superadmin@demo.local`.
3. Avvio (servono **due terminali**): terminale 1 `npm run dev:api` (porta 4000), terminale 2 `npm run dev` (porta 5173). Login: **admin@test.com / admin123** oppure **superadmin@demo.local / admin123**.
4. **Giro di prova veloce nel browser** delle cose fatte: (a) menu → Clienti → scheda "Campi personalizzati": crea un campo, poi apri/crea un cliente e compila il campo, poi guarda il dettaglio; (b) clic su una riga cliente per aprire la scheda; (c) Web Assets → "View" apre la modale, ri-premi per chiudere, prova il chip filtro; (d) Console piattaforma → scheda "Consumi & costi AI" (sarà vuota senza chiamate AI reali, è normale).
5. **Lavoro suggerito per continuare la V3** (già annotato in `03-roadmap-confronto-e-build.md`): includere i campi personalizzati nell'**import/export CSV** dei clienti (oggi l'intestazione CSV è fissa in `server/modules/clients/service.ts`); poi il **layer integrazioni a plugin + connettore Brevo**.
6. Quando tocchi lo schema del DB: **sempre `prisma migrate dev`** + committa il file di migrazione + segnala qui la nuova migrazione. Stili: `npm run lint:css` e `npm run lint:colors`. File `.ts`: `tsc` (restano errori preesistenti in `agency-os`/`web-assets`/`dashboard`/test, non nostri).

## Riferimenti utili

- **Roadmap (dove siamo):** `archivio-documenti/03-roadmap-confronto-e-build.md`. V2 completa; V3 in corso (Custom Fields fatti, Import/Export CSV già esistente, manca Brevo).
- **Console piattaforma:** `src/views/Settings/PlatformConsole.jsx` + cartella `src/views/Settings/platform/`; backend in `server/routes/admin.route.ts` e `server/services/platform-admin.service.ts`.
- **Campi personalizzati:** backend `server/modules/custom-fields/`; frontend `src/modules/customFields/`; render `src/modules/clients/ui/ClientForm.jsx` e `src/views/Clients/ClientDetail.jsx`.
- **Navigazione a schede:** `src/layout/ModuleTabs/ModuleTabs.jsx` + `src/layout/Sidebar/menuUtils.js`.
- **Righe cliccabili:** aiutante `src/utils/rowActivation.js` (per estenderlo ad altre liste in futuro).
- **Errori operativi da evitare:** `archivio-documenti/note-operative-ai.md`.
