# Handoff — staffetta dell'8 luglio 2026, ore 14:29

**Scritto da:** Jacopo
**Per chi riprende:** la prossima sessione (Claudio nel weekend, o Jacopo nei feriali)

---

## ⚠️ DATABASE — LEGGERE PRIMA DI TUTTO (soprattutto Claudio)

Claudio non ha ancora ripreso il CRM da quando Jacopo ha iniziato: quindi il suo database è **indietro** e va allineato prima di lavorare. Ci sono **tre migrazioni pendenti**, da sistemare **in quest'ordine**:

1. `20260706085001_user_theme_avatar_preferences` — è la migrazione "problematica": impacchetta **tutto l'arretrato dell'area Agency** accumulato da inizio marzo con `db push` senza migrazioni (tabelle `ProjectType`, `ProjectMemory`, `ProjectAlert`, `ProjectOpportunity`, `ProjectTask`, `AgencyRuntimeSetting`, ecc.). Se il tuo database **ha già** quelle tabelle, applicarla dà errore *"relation already exists"*.
2. `20260708074026_departments` — nuova (Reparti). Aggiunge solo tabelle nuove, è innocua.
3. `20260708100228_project_visibility_hierarchy` — nuova (visibilità progetti). Aggiunge solo tabelle/colonne nuove, è innocua.

**Come allinearti — due strade:**

- **Strada semplice (consigliata in sviluppo, se puoi perdere i dati locali):**
  `npx prisma migrate reset` — cancella e ricrea il database applicando **tutte** le migrazioni da zero, in ordine. Nessuna riconciliazione manuale, parti pulito. Poi `npm run db:seed:demo` se vuoi i dati demo.
- **Strada che conserva i dati (se il tuo DB ha già le tabelle Agency):**
  prima `npx prisma migrate resolve --applied 20260706085001` (segna come "già applicata" la migrazione problematica senza rieseguirla), poi `npx prisma migrate deploy` (applica le due nuove, Reparti e visibilità).

Regola confermata e scritta in `CLAUDE.md`: **d'ora in poi solo migrazioni tracciate (`prisma migrate dev`), mai `db push`.** È così che si evita di ricreare l'arretrato. **Non riscrivere** le migrazioni già applicate (cambierebbe la loro "impronta" e romperebbe gli ambienti dove funzionano).

---

## Cosa ho fatto

Sessione lunga, tutta sulla **V2 della roadmap**. Chiusi **3 blocchi su 4**. Tutto committato e pushato su `main`.

**Blocco 1 — Editor Ruoli "su misura"** (commit `909eb2d`). Nuova pagina **"Ruoli e permessi"** (menu → Sicurezza) per creare ruoli personalizzati scegliendo i permessi con una griglia a spunta divisa per modulo; i ruoli di sistema restano in sola lettura. Sbloccata anche l'**assegnazione additiva**: un membro tiene un ruolo base tra i 5 di sistema e può avere in più ruoli personalizzati che **sommano** permessi (assegnabili dalla pagina Team). File chiave: `src/views/Settings/RolesSettings.jsx`, `src/modules/roles/api/rolesApi.js`, `server/routes/workspace-roles.route.ts`. Nessuna migrazione.

**Blocco 2 — Reparti** (commit `2169d1a`). Nuova pagina **"Reparti"** (menu → Sicurezza): crea/modifica/elimina reparti (es. Web, Marketing, Social, Grafica, Laboratorio) e assegna i membri. File: `src/views/Settings/DepartmentsSettings.jsx`, modelli `Department`/`DepartmentMember`. **Prima migrazione nuova** (`20260708074026_departments`).

**Blocco 3 — Visibilità pertinente + gerarchia Capo Reparto** (commit `796d4db` backend, `8a0a3f9` frontend). Ora chi non ha visibilità totale vede solo i progetti che lo riguardano:
- ogni reparto può avere un **Capo Reparto** (interruttore nella pagina Reparti); gli altri sono sottoposti;
- **default sottoposto:** vede solo i progetti **assegnati a lui**; **Capo Reparto:** vede tutti i progetti del suo reparto + quelli senza reparto; **Admin/Superadmin/Manager:** vedono tutto (nuovo permesso `projects.view_all`);
- un progetto può appartenere a **uno o più reparti**; nel **dettaglio progetto** c'è un pannello "Accessi & Reparti" per assegnare reparti e scegliere chi lo vede (compare solo a chi può gestirlo);
- il Capo Reparto gestisce gli accessi **solo nel suo perimetro**. File: `server/modules/projects/project-access.service.ts`, filtro in `server/modules/projects/projects.repository.ts`/`projects.service.ts`, pannello `src/modules/projects/ui/detail/ProjectAccessPanel.jsx`. **Seconda migrazione nuova** (`20260708100228_project_visibility_hierarchy`).

Aggiornata anche la roadmap (`archivio-documenti/03-roadmap-confronto-e-build.md`): annotato come "da fare più avanti" il controllo fine **vedere vs modificare** per singolo progetto (per ora l'accesso è binario: vede/non vede).

## Dove mi sono fermato

- **Tutto committato e pushato.** Ultimo commit: `8a0a3f9`. Albero pulito.
- **Backend del blocco 3 verificato end-to-end** con un utente ristretto reale: da sottoposto vede solo il progetto assegnato; promosso a Capo Reparto vede anche i progetti del reparto e quelli senza reparto. Tutti i test automatici backend verdi (145).
- **Frontend verificato solo in compilazione** (Vite compila i nuovi file senza errori). Le pagine nuove non sono state "cliccate" in un browser loggato perché in anteprima il login è scomodo (l'app parla con l'API su un'altra porta). Conviene una prova manuale nel browser già loggato: menu Sicurezza → **Ruoli e permessi** e **Reparti**; poi apri un progetto e guarda il pannello **Accessi & Reparti** in fondo.
- **Cambio di comportamento da sapere:** appena il database è allineato, gli utenti **Operativo/Viewer** senza assegnazioni vedranno la lista progetti **vuota** (è il default restrittivo voluto). Per far vedere loro un progetto: assegnalo a loro (o al loro reparto con loro come capo) dal pannello "Accessi & Reparti" del progetto. Admin/Superadmin/Manager continuano a vedere tutto.
- **Manca il Blocco 4 della V2:** la **Console Super Admin globale** (gestione dei workspace dall'alto: creare/sospendere/configurare workspace, log costi AI). È il pezzo più grande e diverso, lasciato apposta a una sessione dedicata.

## Come riprendere — prossimi passi

1. **`git pull`** per avere l'ultimo commit (`8a0a3f9`).
2. **Allinea il database** seguendo l'avviso in cima (se sei Claudio: quasi certamente ti serve, scegli "strada semplice" o "conserva i dati").
3. Avvio: `npm run dev:api` (porta 4000) + `npm run dev` (porta 5173). Login di prova: **admin@test.com / admin123**.
4. **Prova le pagine nuove** nel browser (vedi sopra) per confermare che tutto giri con dati reali.
5. **Prosegui la V2 con il Blocco 4** (Console Super Admin) oppure rifinisci quanto fatto. Vedi la sezione V2 della roadmap.
6. Quando tocchi lo schema del DB: **sempre `prisma migrate dev`** + committa il file di migrazione + segnala qui la nuova migrazione. Stili: `npm run lint:css` e `npm run lint:colors` (restano 11 avvisi preesistenti in `clients-ui.css`, non nostri). File `.ts`: `tsc` (restano errori preesistenti in `agency-os`/`seed.ts`, non nostri).

## Riferimenti utili

- **Roadmap (dove siamo: V2, 3 blocchi su 4 fatti):** `archivio-documenti/03-roadmap-confronto-e-build.md`.
- **Ruoli:** pagina `src/views/Settings/RolesSettings.jsx`; API `src/modules/roles/api/rolesApi.js`; backend `server/routes/workspace-roles.route.ts` + `server/services/workspace-roles.service.ts`.
- **Reparti:** pagina `src/views/Settings/DepartmentsSettings.jsx`; API `src/modules/departments/api/departmentsApi.js`; backend `server/routes/workspace-departments.route.ts` + `server/services/workspace-departments.service.ts`.
- **Visibilità progetti:** logica `server/modules/projects/project-access.service.ts` + filtro in `server/modules/projects/projects.repository.ts`/`projects.service.ts`; pannello UI `src/modules/projects/ui/detail/ProjectAccessPanel.jsx`.
- **Errori operativi da evitare:** `archivio-documenti/note-operative-ai.md` (voce §10: come verificare flussi con login testando l'API via `curl` invece di lottare con l'anteprima).
