# Handoff — staffetta del 2 luglio 2026, ore 15:19

**Scritto da:** Jacopo
**Per chi riprende:** Claudio

## Cosa ho fatto

Sessione lunga, quasi tutta su **tema chiaro/scuro e design system**. Tutto committato e pushato su `main` (ultimo commit `1623476`). In ordine:

1. **Pulizia colori accento.** Le variabili dell'accento si chiamavano ancora `--brand-yellow*` pur essendo blu: rinominate in `--brand-accent*` (in `globals.css`, `clients-ui.css` e `workspaceBranding.ts`, incluso il punto dove il colore viene applicato a runtime). Rimossi anche dei gialli "veri" scritti a mano rimasti nel codice (aloni, badge), sostituiti con l'accento. Restano gialli solo dove servono davvero (palette del template, bandiere).

2. **Tema di default = sistema operativo.** Prima partiva sempre chiaro; ora segue il tema del PC (chiaro/scuro). Se l'utente sceglie un tema con l'interruttore, quella scelta viene salvata e vince.

3. **Pagine di accesso adattive al tema.** Login, Registrazione (Signup) e Accetta-invito forzavano lo sfondo scuro anche in tema chiaro: corretto, ora seguono il tema. Nel Signup il logo Advaiora si inverte (nero su chiaro, bianco su scuro).

4. **Regole per non ripetere gli errori e per i colori.**
   - Creato `archivio-documenti/note-operative-ai.md`: file dove l'AI annota da sola i propri errori operativi ricorrenti (es. come fare le verifiche in anteprima senza perdere tempo).
   - Creato `archivio-documenti/design-system-temi.md`: la "regola d'oro" (usare sempre i token colore `var(--…)`, mai colori scritti a mano) + l'elenco dei token.
   - Aggiunti due controlli automatici: `npm run lint:css` (colori fissi nei CSS dei moduli) e `npm run lint:colors` (colori fissi negli stili scritti dentro il JSX). Entrambi solo su `src/modules/**`, come avvisi.
   - Aggiornato `CLAUDE.md` con i rimandi a questi documenti/comandi.

5. **Roadmap aggiornata** (`archivio-documenti/03-roadmap-confronto-e-build.md`): aggiunte due voci — la definizione futura degli **schemi colore della brandizzazione** (dentro V1) e una sezione **"Debito tecnico / tooling"** con la migrazione ESLint (vedi sotto).

6. **Iniziato il Task 2 della V1 (rifinitura Apple-style dei componenti): la sidebar è ora adattiva al tema** (bianca in chiaro, scura in scuro, come Finder/Impostazioni). Prima era grigia fissa perché colorata dal "colore secondario" del branding: l'ho scollegata da lì e agganciata ai token del tema.

## Dove mi sono fermato

- **Tutto stabile e pushato su `main`** (ultimo commit `1623476`). App funzionante, login OK, verifiche visive fatte.
- **Task 2 solo iniziato.** Fatto: sidebar adattiva. **Restano da rifinire con i token Apple** (sostituire valori scritti a mano con quelli del design system, lavoro a basso rischio):
  - card / menu a tendina / finestre: ombre e raggi → token (`--shadow-sm/md`, `--radius-md/lg`);
  - bottoni: raggio da token + micro-transizione + ombra tenue sul primario;
  - barra in alto (topnav): bordo più sottile / leggera trasparenza.
- **Due cose delicate da sapere:**
  1. **Il controllo `npm run lint` (JavaScript) è rotto da prima**, non per colpa di questa sessione: ESLint richiede un nuovo formato di configurazione non ancora migrato. È tracciato nella roadmap (sezione "Debito tecnico / tooling") e come attività suggerita. **I nostri due controlli colore (`lint:css`, `lint:colors`) funzionano lo stesso**, sono autonomi.
  2. **L'anteprima in locale è lenta a ricaricarsi dopo ogni modifica ai file di tema/condivisi.** Causa certificata e scritta in `note-operative-ai.md`: modificando file come `globals.css` o `workspaceBranding.ts`, l'app fa un ricaricamento completo (non un aggiornamento "a caldo") per un'incompatibilità di `session-provider.tsx`. Non è un bug del nostro lavoro. Nel documento c'è anche il possibile rimedio, da fare in una sessione dedicata.

## Come riprendere — prossimi passi

1. **Avvia l'app in locale** (due terminali nella cartella del progetto):
   - se serve, prima `npm install`;
   - terminale 1: `npm run dev:api` (backend, porta 4000);
   - terminale 2: `npm run dev` (frontend, porta 5173);
   - login di prova: **admin@test.com / admin123**.
   - Nota: al primo avvio dopo un `npm install`, Vite ri-ottimizza le dipendenze e la pagina resta vuota ~10-30 s: è normale, **aspetta senza ricaricare**.
2. **Continua il Task 2** portando ombre/raggi/transizioni di **card, menu a tendina, finestre, bottoni e topnav** sui token Apple in `src/styles/scss/globals.css`. La sidebar è già fatta come riferimento di stile.
3. Quando tocchi stili di moduli o componenti, lancia `npm run lint:css` e `npm run lint:colors` per non reintrodurre colori fissi.
4. Chiuso il Task 2, restano i **Task 3-6** della V1 (ricerca globale "Command-K", scorciatoie, preferenze utente, menu contestuale per ruolo).
5. A fine sessione, genera il nuovo handoff con `/handoff`.

## Riferimenti utili

- Regole colori/tema e token: `archivio-documenti/design-system-temi.md`
- Errori operativi da evitare (incl. lentezza anteprima): `archivio-documenti/note-operative-ai.md`
- Roadmap 10 tappe + debito tecnico: `archivio-documenti/03-roadmap-confronto-e-build.md`
- Visione prodotto ("la bibbia"): `archivio-documenti/02-brief-operativo-definitivo-bibbia.md`
- Colori/tema dell'app: `src/styles/scss/globals.css` (+ token in `src/styles/design-tokens.css`) e `src/lib/workspaceBranding.ts` (applica il branding a runtime)
- Contratto di lavoro condiviso: `CLAUDE.md`
- Avvio e comandi: `installazione-e-avvio.md`
