# Handoff — staffetta del 23 luglio 2026, ore 17:49

**Scritto da:** Jacopo (settimana)
**Per chi riprende:** Claudio

## Cosa ho fatto

Tutto committato e pushato su `main`. Tre commit oggi pomeriggio: `182c877`, `0643bbc` e `dc63af2`.

1. **Creato un team di tre assistenti secondari ("agent") per lo sviluppo** — commit `dc63af2`. Sono in `.claude/agents/`, si attivano da soli e **nessuno dei tre può modificare file**: non hanno gli strumenti per farlo.
   - **`esploratore`** — si chiama **prima** di scrivere codice. Torna la mappa dei file da toccare e, soprattutto, l'elenco completo dei punti da collegare (catalogo permessi, registrazione rotte, voci di menu, costanti frontend). Serve contro l'errore più frequente qui: una cosa collegata in alcuni punti e non in tutti, che funziona a metà senza dare errore.
   - **`revisore`** — controlla il lavoro **non ancora committato**. Non è legato al commit: si chiama a ogni tappa conclusa (dopo schema+migrazione, dopo il collegamento, e in chiusura). Cerca gli errori tipici di questo progetto, comprese le trappole delle note operative #21, #30 e #32.
   - **`architetto`** — misura i consumi e **propone** modifiche al team (aggiungere, cambiare, spegnere un agent), sempre con una stima di costo. Non applica mai niente: le modifiche approvate le applica la sessione principale.

2. **Aggiunto un misuratore dei consumi** — `scripts/agenti/consumi.mjs`. Si lancia con `node scripts/agenti/consumi.mjs` e dice quanto si è consumato nella finestra di 5 ore in corso, qual è il picco storico e quanta parte del consumo è finita negli agent. Legge solo registri locali della propria macchina: non manda niente in rete e non scrive alcun file (a meno di aggiungere `--scrivi`).
   - **Perché serve:** si lavora su abbonamento MAX 5x, quindi il problema non sono i costi in denaro ma **saturare la finestra di consumo e prendere un blocco a metà lavoro**.
   - **Dato utile emerso dalla misura sulle prime 38 sessioni:** il **56% del consumo è rilettura della cache**, cioè la conversazione ripresentata al modello a ogni turno. È il motivo per cui gli agent qui convengono: quello che leggono resta nel loro contesto e non appesantisce la sessione principale per tutto il resto della giornata.

3. **Scritta la documentazione** — `archivio-documenti/team-agenti.md`. Contiene com'è fatto il team, i numeri di partenza, la procedura dei consumi, **l'archivio delle alternative scartate** (altre configurazioni valutate, agent proposti e non adottati, cose che si è deciso di non trasformare in agent, con i motivi) e il registro delle decisioni. In `CLAUDE.md` è rimasto solo un rimando corto, per non appesantire ogni sessione.

4. **Modificato il comando `/handoff`**: ora, se serve, chiede la lettura del consumo reale. La percentuale del limite dell'abbonamento **non è leggibile in automatico** — verificato, non c'è né un comando né un file locale che la esponga — quindi va letta a mano con `/usage` nell'app e registrata. Con 3-5 letture il misuratore impara a convertire e parla in percentuale invece che in relativo.

5. **Resoconto dello stato di avanzamento in HTML** — commit `182c877` e `0643bbc`, fatti in una sessione parallela: `archivio-documenti/resoconto-avanzamento.html`, documento aggiornabile con anche gli stili per l'esportazione in PDF.

## Dove mi sono fermato

- **Tutto completo, committato e pushato.** Niente lasciato a metà, nessun file non committato.
- **Nessuna migrazione del database** in questa sessione. Restano le nove già segnalate a te negli handoff precedenti.
- **Nessuna modifica al codice del CRM**: si è toccata solo la configurazione di lavoro e la documentazione.
- ⚠️ **Stato dei dev server: DUE RESTANO ACCESI, e non sono di questa sessione.** Porta 4000 (l'API, processo 15496) e porta 5173 (il frontend, processo 13608), entrambi avviati oggi alle 17:13. Non li ho spenti perché la regola dice di non terminare mai server che appartengono a un'altra finestra ancora al lavoro. **Se apri una sessione e ti servono migrazioni o `prisma generate`, controlla prima queste due porte**: l'API accesa tiene un blocco su Prisma e le migrazioni si piantano.
- **Il misuratore dei consumi non parla ancora in percentuale**: servono le prime letture manuali. Finché mancano, dà comunque un confronto con la propria storia (picco in 5 ore: 117,7 unità; sessione mediana: 42,3).

## Come riprendere — prossimi passi

1. **Avvio** (una sola sessione con i server accesi per volta): `npm run dev:api` sulla porta 4000 e `npm run dev` sulla 5173. Verifica prima che le due porte segnalate qui sopra si siano liberate.
2. **Prova gli agent al primo lavoro vero.** Il modo previsto: chiedere all'`esploratore` la mappa **prima** di scrivere, poi scrivere, poi passare il lavoro al `revisore` a tappa conclusa. Servono due chiamate al revisore per pezzo di lavoro, non una a fine giornata.
3. **Il seguito naturale sul codice, ancora aperto (verificato adesso): estendere lo schema JSON alle altre generazioni AI.** Oggi lo schema che obbliga Claude a produrre JSON valido è attivo **solo** sulla generazione del brief Discovery (`agency.service.ts` riga 9445). Le altre quattro generazioni — Discovery per singola sezione, Web progetto, Web blocco, Ads — chiamano lo stesso motore **senza** passare lo schema, quindi per loro il rischio del JSON non valido esiste ancora. La ricetta è già scritta e collaudata: si guarda com'è fatto `DISCOVERY_AI_JSON_SCHEMA` e se ne scrive uno uguale per ciascuna, elencando i suoi campi.
   - **Trappola da ricordare:** lo schema deve elencare davvero i campi attesi. Uno schema generico fa rispondere a Claude un oggetto vuoto che il sistema registra come "AI usata" (nota operativa #32).
   - **Altra trappola:** la Discovery tiene in cache il risultato per lo stesso progetto e le stesse fonti, quindi ri-generare non richiama l'AI. Per collaudare davvero, usare un progetto diverso.
4. **Decisione ancora aperta, da prendere con te: la Discovery deve inferire o no.** Il primo dato raccolto dice che con l'AI realmente usata la Discovery **inferisce già** il target e dichiara cosa va validato, quindi si comporta già vicino a quello che si voleva. Conviene guardarla su un paio di progetti in più prima di decidere se cambiare qualcosa.
5. **Altri pezzi di V6 ancora da fare:** generazione visiva Higgsfield, e l'import di dati esterni (es. Google Ads) dentro il report PDF — quest'ultimo è grosso perché il collegamento a Google Ads non esiste e va costruito da zero.
6. **Quando capita una sessione carica, registra una lettura dei consumi:** scrivi `/usage` nell'app e passa la percentuale all'assistente, che la mette in `archivio-documenti/consumi/calibrazione.json`. Ne servono 3-5, prese a livelli di carico diversi.

## Riferimenti utili

- **Il team di agent:** `archivio-documenti/team-agenti.md` — il documento da leggere per capire cosa esiste, perché, e cosa era stato scartato. I file degli agent sono in `.claude/agents/`.
- **Consumi:** `node scripts/agenti/consumi.mjs` — il quadro dei consumi. Con `--scrivi` aggiorna anche il registro condiviso.
- **Structured output di Claude:** `server/modules/agency-os/anthropic-json.ts` e il motore `runAgencyOpenAiJsonWithMeta` in `server/modules/agency-os/agency.service.ts`.
- **Note operative:** `archivio-documenti/note-operative-ai.md` — in particolare #21 (collegare a tutte le rotte), #28 (server accesi che bloccano Prisma), #30 e #32 (AI che ripiega o mente in silenzio).
- **Resoconto avanzamento:** `archivio-documenti/resoconto-avanzamento.html` — documento aggiornabile, apribile nel browser.
